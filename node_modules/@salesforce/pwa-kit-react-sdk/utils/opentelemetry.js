"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.logSpanData = exports.endSpan = exports.createSpan = exports.createChildSpan = void 0;
var _api = require("@opentelemetry/api");
var _core = require("@opentelemetry/core");
var _loggerInstance = _interopRequireDefault(require("./logger-instance"));
var _opentelemetryConfig = require("./opentelemetry-config");
const _excluded = ["performance_mark", "performance_detail"];
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = _objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function _objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); } /*
 * Copyright (c) 2025, Salesforce, Inc.
 * All rights reserved.
 * SPDX-License-Identifier: BSD-3-Clause
 * For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/BSD-3-Clause
 */
const logSpanData = (span, event = 'start') => {
  var _span$parentSpanConte;
  if (!(0, _opentelemetryConfig.getOTELConfig)().enabled) {
    return;
  }
  const spanContext = span.spanContext();
  const startTime = span.startTime;
  const endTime = event === 'start' ? startTime : span.endTime;
  const duration = event === 'start' ? 0 : span.duration;
  if (!Array.isArray(startTime) || startTime.length !== 2 || duration !== 0 && (!Array.isArray(duration) || duration.length !== 2)) {
    _loggerInstance.default.warn('Invalid timing data detected - OpenTelemetry may not be properly initialized', {
      namespace: 'opentelemetry',
      additionalProperties: {
        span_name: span.name,
        event: event,
        startTime_valid: Array.isArray(startTime) && startTime.length === 2,
        duration_valid: duration === 0 || Array.isArray(duration) && duration.length === 2,
        otel_enabled: (0, _opentelemetryConfig.getOTELConfig)().enabled,
        startTime_type: typeof startTime,
        startTime_value: startTime
      }
    });
  }

  // Create the span data object that matches the expected format
  const spanData = {
    traceId: spanContext.traceId,
    parentId: (_span$parentSpanConte = span.parentSpanContext) === null || _span$parentSpanConte === void 0 ? void 0 : _span$parentSpanConte.spanId,
    name: span.name,
    id: spanContext.spanId,
    kind: span.kind,
    timestamp: startTime ? (0, _core.hrTimeToTimeStamp)(startTime) : undefined,
    duration: duration,
    attributes: _objectSpread(_objectSpread({
      'service.name': (0, _opentelemetryConfig.getServiceName)()
    }, span.attributes), {}, {
      event: event // Add event type to distinguish start/end
    }),
    status: {
      code: event === 'start' ? _api.SpanStatusCode.UNSET : _api.SpanStatusCode.OK
    },
    events: [],
    links: [],
    start_time: startTime,
    end_time: endTime,
    forwardTrace: (0, _opentelemetryConfig.getOTELConfig)().enabled
  };
  if (event === 'end') {
    console.info(JSON.stringify(spanData));
  }
};

/**
 * Creates a new span with the given name and options
 * @param {string} name - The name of the span
 * @param {Object} options - Span options
 * @returns {Span} The created span
 */
exports.logSpanData = logSpanData;
const createSpan = (name, options = {}) => {
  try {
    const tracer = _api.trace.getTracer((0, _opentelemetryConfig.getServiceName)());

    // Create a new span with the current context
    const span = tracer.startSpan(name, _objectSpread(_objectSpread({}, options), {}, {
      attributes: _objectSpread(_objectSpread({}, options.attributes), {}, {
        'service.name': (0, _opentelemetryConfig.getServiceName)()
      })
    }), _api.context.active());
    return _api.trace.setSpan(_api.context.active(), span);
  } catch (error) {
    _loggerInstance.default.error('Failed to create span', {
      namespace: 'opentelemetry',
      additionalProperties: {
        spanName: name,
        error: error.message
      }
    });
    return null;
  }
};

/**
 * Creates a child span with the given name and attributes
 * @param {string} name - The name of the span
 * @param {Object} attributes - The attributes to add to the span
 * @returns {Span} The created span
 */
exports.createSpan = createSpan;
const createChildSpan = (name, attributes = {}) => {
  try {
    var _parentSpan$attribute;
    // Check if OpenTelemetry is properly configured
    const otelConfig = (0, _opentelemetryConfig.getOTELConfig)();
    if (!otelConfig.enabled) {
      _loggerInstance.default.warn('OpenTelemetry is disabled - spans will not have proper timing data', {
        namespace: 'opentelemetry',
        additionalProperties: {
          span_name: name,
          otel_enabled: otelConfig.enabled,
          otel_service_name: otelConfig.serviceName,
          suggestion: 'Set OTEL_TRACING_ENABLED=true to enable proper timing'
        }
      });
    }
    const tracer = _api.trace.getTracer((0, _opentelemetryConfig.getServiceName)());
    const ctx = _api.context.active();
    const parentSpan = _api.trace.getSpan(ctx);

    // Don't create duplicate spans
    if ((parentSpan === null || parentSpan === void 0 ? void 0 : (_parentSpan$attribute = parentSpan.attributes) === null || _parentSpan$attribute === void 0 ? void 0 : _parentSpan$attribute.performance_mark) === name) {
      return parentSpan;
    }
    const {
        performance_mark,
        performance_detail
      } = attributes,
      otherAttributes = _objectWithoutProperties(attributes, _excluded);
    const spanAttributes = _objectSpread({
      'service.name': (0, _opentelemetryConfig.getServiceName)()
    }, otherAttributes);
    if (performance_mark) {
      spanAttributes['performance.mark'] = performance_mark;
      spanAttributes['performance.type'] = 'start';
      spanAttributes['performance.detail'] = typeof performance_detail === 'string' ? performance_detail : JSON.stringify(performance_detail);
    }
    const span = tracer.startSpan(name, {
      attributes: spanAttributes
    }, parentSpan ? ctx : undefined);
    return span;
  } catch (error) {
    _loggerInstance.default.error('Error creating OpenTelemetry span', {
      namespace: 'opentelemetry',
      additionalProperties: {
        spanName: name,
        error: error.message
      }
    });
    return null;
  }
};

/**
 * Ends a span and logs its data
 * @param {Span} span - The span to end
 */
exports.createChildSpan = createChildSpan;
const endSpan = span => {
  if (!span) {
    return;
  }
  try {
    span.end();

    // Log completion data
    logSpanData(span, 'end');
  } catch (error) {
    _loggerInstance.default.error('Error ending OpenTelemetry span', {
      namespace: 'opentelemetry',
      additionalProperties: {
        error: error.message
      }
    });
  }
};
exports.endSpan = endSpan;