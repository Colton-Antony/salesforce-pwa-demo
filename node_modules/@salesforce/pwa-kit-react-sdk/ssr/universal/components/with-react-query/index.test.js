"use strict";

var _index = require("./index");
var _react = require("@testing-library/react");
var _react2 = _interopRequireDefault(require("react"));
var _loggerInstance = _interopRequireDefault(require("../../../../utils/logger-instance"));
var _performanceMarks = require("../../../../utils/performance-marks");
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); } /*
 * Copyright (c) 2022, Salesforce, Inc.
 * All rights reserved.
 * SPDX-License-Identifier: BSD-3-Clause
 * For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/BSD-3-Clause
 */
jest.mock('@tanstack/react-query', () => _objectSpread(_objectSpread({}, jest.requireActual('@tanstack/react-query')), {}, {
  dehydrate: jest.fn().mockReturnValue({})
}));
jest.mock('../../../../utils/logger-instance', () => {
  return {
    error: jest.fn()
  };
});
describe('withReactQuery', function () {
  let windowSpy;
  let oldPreloadedState = window.__PRELOADED_STATE__;
  beforeEach(() => {
    windowSpy = jest.spyOn(window, 'window', 'get');
    jest.spyOn(console, 'warn').mockImplementation(jest.fn());
  });
  afterEach(() => {
    console.warn.mockRestore();
    windowSpy.mockRestore();
    window.__PRELOADED_STATE__ = oldPreloadedState;
  });
  test('Renders correctly', () => {
    const Wrapped = () => /*#__PURE__*/_react2.default.createElement("p", null, "Hello world");
    const Component = (0, _index.withReactQuery)(Wrapped);
    (0, _react.render)(/*#__PURE__*/_react2.default.createElement(Component, {
      locals: {}
    }));
    expect(_react.screen.getByText(/Hello world/i)).toBeInTheDocument();
  });
  test('`beforeHydrate` called on mount', () => {
    const mockPreloadedState = {
      payload: {}
    };
    const mockBeforeHydrate = jest.fn();
    const Wrapped = () => /*#__PURE__*/_react2.default.createElement("p", null, "Hello world");
    const Component = (0, _index.withReactQuery)(Wrapped, {
      beforeHydrate: mockBeforeHydrate
    });
    window.__PRELOADED_STATE__ = {
      __reactQuery: mockPreloadedState
    };
    (0, _react.render)(/*#__PURE__*/_react2.default.createElement(Component, {
      locals: {}
    }));
    expect(_react.screen.getByText(/Hello world/i)).toBeInTheDocument();
    expect(mockBeforeHydrate).toHaveBeenCalledTimes(1);
    expect(mockBeforeHydrate).toHaveBeenCalledWith(mockPreloadedState);
  });
  test('Renders correctly when `beforeHydrate` throws', () => {
    const mockPreloadedState = {
      payload: {}
    };
    const mockError = new Error('Test Error');
    const mockBeforeHydrate = jest.fn().mockImplementation(() => {
      throw mockError;
    });
    const Wrapped = () => /*#__PURE__*/_react2.default.createElement("p", null, "Hello world");
    const Component = (0, _index.withReactQuery)(Wrapped, {
      beforeHydrate: mockBeforeHydrate
    });
    window.__PRELOADED_STATE__ = {
      __reactQuery: mockPreloadedState
    };
    _loggerInstance.default.error = jest.fn();
    (0, _react.render)(/*#__PURE__*/_react2.default.createElement(Component, {
      locals: {}
    }));
    expect(_react.screen.getByText(/Hello world/i)).toBeInTheDocument();
    expect(mockBeforeHydrate).toHaveBeenCalledTimes(1);
    expect(mockBeforeHydrate).toHaveBeenCalledWith(mockPreloadedState);
    expect(_loggerInstance.default.error).toHaveBeenCalledTimes(1);
    expect(_loggerInstance.default.error).toHaveBeenCalledWith('Client `beforeHydrate` failed', {
      additionalProperties: {
        error: mockError
      },
      namespace: 'with-react-query.render'
    });
  });
  test(`Has working getInitializers method`, () => {
    expect((0, _index.withReactQuery)({}).getInitializers()).toHaveLength(1);
    expect((0, _index.withReactQuery)({
      getInitializers: () => ['xyz']
    }).getInitializers()).toHaveLength(2);
  });
  test(`Has working getHOCsInUse method`, () => {
    expect((0, _index.withReactQuery)({}).getHOCsInUse()).toHaveLength(1);
    expect((0, _index.withReactQuery)({
      getHOCsInUse: () => ['xyz']
    }).getHOCsInUse()).toHaveLength(2);
  });
  test('Performance markers use hyphen as delimiter for displayName', /*#__PURE__*/_asyncToGenerator(function* () {
    const mockPerformanceTimer = {
      mark: jest.fn()
    };
    const mockQueryMeta = {
      displayName: 'TestQuery'
    };
    const mockQueryCache = {
      getAll: jest.fn().mockReturnValue([{
        options: {
          enabled: true
        },
        meta: mockQueryMeta,
        queryHash: 'test-hash',
        fetch: jest.fn().mockResolvedValue({})
      }])
    };
    const mockQueryClient = {
      getQueryCache: jest.fn().mockReturnValue(mockQueryCache)
    };
    const res = {
      locals: {
        __queryClient: mockQueryClient
      },
      __performanceTimer: mockPerformanceTimer
    };
    const Component = (0, _index.withReactQuery)({});
    yield Component.doInitAppState({
      res,
      appJSX: /*#__PURE__*/_react2.default.createElement("div", null, "Test")
    });
    expect(mockPerformanceTimer.mark).toHaveBeenCalledWith(`${_performanceMarks.PERFORMANCE_MARKS.reactQueryUseQuery}.TestQuery-0`, 'start');
    expect(mockPerformanceTimer.mark).toHaveBeenCalledWith(`${_performanceMarks.PERFORMANCE_MARKS.reactQueryUseQuery}.TestQuery-0`, 'end', expect.objectContaining({
      detail: 'test-hash'
    }));
  }));
  test('Performance markers use index as displayName when meta.displayName is not available', /*#__PURE__*/_asyncToGenerator(function* () {
    const mockPerformanceTimer = {
      mark: jest.fn()
    };
    const mockQueryCache = {
      getAll: jest.fn().mockReturnValue([{
        options: {
          enabled: true
        },
        meta: {},
        queryHash: 'test-hash',
        fetch: jest.fn().mockResolvedValue({})
      }])
    };
    const mockQueryClient = {
      getQueryCache: jest.fn().mockReturnValue(mockQueryCache)
    };
    const res = {
      locals: {
        __queryClient: mockQueryClient
      },
      __performanceTimer: mockPerformanceTimer
    };
    const Component = (0, _index.withReactQuery)({});
    yield Component.doInitAppState({
      res,
      appJSX: /*#__PURE__*/_react2.default.createElement("div", null, "Test")
    });

    // Verify the performance marker uses just the index when no displayName is available
    expect(mockPerformanceTimer.mark).toHaveBeenCalledWith(`${_performanceMarks.PERFORMANCE_MARKS.reactQueryUseQuery}.0`, 'start');
    expect(mockPerformanceTimer.mark).toHaveBeenCalledWith(`${_performanceMarks.PERFORMANCE_MARKS.reactQueryUseQuery}.0`, 'end', expect.objectContaining({
      detail: 'test-hash'
    }));
  }));
  test('Performance markers for reactQueryPrerender are set correctly', /*#__PURE__*/_asyncToGenerator(function* () {
    const mockPerformanceTimer = {
      mark: jest.fn()
    };
    const mockQueryCache = {
      getAll: jest.fn().mockReturnValue([])
    };
    const mockQueryClient = {
      getQueryCache: jest.fn().mockReturnValue(mockQueryCache)
    };
    const res = {
      locals: {
        __queryClient: mockQueryClient
      },
      __performanceTimer: mockPerformanceTimer
    };
    const Component = (0, _index.withReactQuery)({});
    yield Component.doInitAppState({
      res,
      appJSX: /*#__PURE__*/_react2.default.createElement("div", null, "Test")
    });

    // Verify the performance markers for reactQueryPrerender are set correctly
    expect(mockPerformanceTimer.mark).toHaveBeenCalledWith(_performanceMarks.PERFORMANCE_MARKS.reactQueryPrerender, 'start');
    expect(mockPerformanceTimer.mark).toHaveBeenCalledWith(_performanceMarks.PERFORMANCE_MARKS.reactQueryPrerender, 'end');
  }));
  test('Performance marker is closed regardless of fetch status - success case', /*#__PURE__*/_asyncToGenerator(function* () {
    const mockPerformanceTimer = {
      mark: jest.fn()
    };
    const mockQueryCache = {
      getAll: jest.fn().mockReturnValue([{
        options: {
          enabled: true
        },
        meta: {
          displayName: 'SuccessQuery'
        },
        queryHash: 'success-hash',
        fetch: jest.fn().mockResolvedValue({
          data: 'success'
        })
      }])
    };
    const mockQueryClient = {
      getQueryCache: jest.fn().mockReturnValue(mockQueryCache)
    };
    const res = {
      locals: {
        __queryClient: mockQueryClient
      },
      __performanceTimer: mockPerformanceTimer
    };
    const Component = (0, _index.withReactQuery)({});
    yield Component.doInitAppState({
      res,
      appJSX: /*#__PURE__*/_react2.default.createElement("div", null, "Test")
    });

    // Verify timer start is called
    expect(mockPerformanceTimer.mark).toHaveBeenCalledWith(`${_performanceMarks.PERFORMANCE_MARKS.reactQueryUseQuery}.SuccessQuery-0`, 'start');

    // Verify timer end is called even on successful fetch
    expect(mockPerformanceTimer.mark).toHaveBeenCalledWith(`${_performanceMarks.PERFORMANCE_MARKS.reactQueryUseQuery}.SuccessQuery-0`, 'end', expect.objectContaining({
      detail: 'success-hash'
    }));
  }));
  test('Performance marker is ended regardless of fetch status - error case', /*#__PURE__*/_asyncToGenerator(function* () {
    const mockPerformanceTimer = {
      mark: jest.fn()
    };
    const mockError = new Error('Fetch failed');
    const mockQueryCache = {
      getAll: jest.fn().mockReturnValue([{
        options: {
          enabled: true
        },
        meta: {
          displayName: 'ErrorQuery'
        },
        queryHash: 'error-hash',
        fetch: jest.fn().mockRejectedValue(mockError)
      }])
    };
    const mockQueryClient = {
      getQueryCache: jest.fn().mockReturnValue(mockQueryCache)
    };
    const res = {
      locals: {
        __queryClient: mockQueryClient
      },
      __performanceTimer: mockPerformanceTimer
    };
    const Component = (0, _index.withReactQuery)({});

    // Reset the logger mock to ensure clean state
    _loggerInstance.default.error.mockClear();
    yield Component.doInitAppState({
      res,
      appJSX: /*#__PURE__*/_react2.default.createElement("div", null, "Test")
    });

    // Verify timer start is called
    expect(mockPerformanceTimer.mark).toHaveBeenCalledWith(`${_performanceMarks.PERFORMANCE_MARKS.reactQueryUseQuery}.ErrorQuery-0`, 'start');

    // Verify timer end is called even when fetch fails
    expect(mockPerformanceTimer.mark).toHaveBeenCalledWith(`${_performanceMarks.PERFORMANCE_MARKS.reactQueryUseQuery}.ErrorQuery-0`, 'end', expect.objectContaining({
      detail: 'error-hash'
    }));
  }));
  test('Query errors are logged properly during SSR', /*#__PURE__*/_asyncToGenerator(function* () {
    const mockPerformanceTimer = {
      mark: jest.fn()
    };
    const mockError = new Error('Network timeout');
    const mockQueryCache = {
      getAll: jest.fn().mockReturnValue([{
        options: {
          enabled: true
        },
        meta: {
          displayName: 'FailingQuery'
        },
        queryHash: 'failing-hash',
        fetch: jest.fn().mockRejectedValue(mockError)
      }])
    };
    const mockQueryClient = {
      getQueryCache: jest.fn().mockReturnValue(mockQueryCache)
    };
    const res = {
      locals: {
        __queryClient: mockQueryClient
      },
      __performanceTimer: mockPerformanceTimer
    };
    const Component = (0, _index.withReactQuery)({});

    // Reset the logger mock to ensure clean state
    _loggerInstance.default.error.mockClear();
    yield Component.doInitAppState({
      res,
      appJSX: /*#__PURE__*/_react2.default.createElement("div", null, "Test")
    });

    // Verify that the error is logged with correct parameters
    expect(_loggerInstance.default.error).toHaveBeenCalledTimes(1);
    expect(_loggerInstance.default.error).toHaveBeenCalledWith('Query during SSR results in an error', {
      namespace: 'with-react-query.doInitAppState',
      additionalProperties: {
        queryHash: 'failing-hash',
        error: mockError
      }
    });
  }));
  test('Multiple queries with mixed success/error status - all performance markers ended and errors logged', /*#__PURE__*/_asyncToGenerator(function* () {
    const mockPerformanceTimer = {
      mark: jest.fn()
    };
    const mockError = new Error('Second query failed');
    const mockQueryCache = {
      getAll: jest.fn().mockReturnValue([{
        options: {
          enabled: true
        },
        meta: {
          displayName: 'SuccessQuery'
        },
        queryHash: 'success-hash',
        fetch: jest.fn().mockResolvedValue({
          data: 'success'
        })
      }, {
        options: {
          enabled: true
        },
        meta: {
          displayName: 'ErrorQuery'
        },
        queryHash: 'error-hash',
        fetch: jest.fn().mockRejectedValue(mockError)
      }, {
        options: {
          enabled: true
        },
        meta: {},
        queryHash: 'another-success-hash',
        fetch: jest.fn().mockResolvedValue({
          data: 'another success'
        })
      }])
    };
    const mockQueryClient = {
      getQueryCache: jest.fn().mockReturnValue(mockQueryCache)
    };
    const res = {
      locals: {
        __queryClient: mockQueryClient
      },
      __performanceTimer: mockPerformanceTimer
    };
    const Component = (0, _index.withReactQuery)({});

    // Reset the logger mock to ensure clean state
    _loggerInstance.default.error.mockClear();
    yield Component.doInitAppState({
      res,
      appJSX: /*#__PURE__*/_react2.default.createElement("div", null, "Test")
    });

    // Verify all timers are started
    expect(mockPerformanceTimer.mark).toHaveBeenCalledWith(`${_performanceMarks.PERFORMANCE_MARKS.reactQueryUseQuery}.SuccessQuery-0`, 'start');
    expect(mockPerformanceTimer.mark).toHaveBeenCalledWith(`${_performanceMarks.PERFORMANCE_MARKS.reactQueryUseQuery}.ErrorQuery-1`, 'start');
    expect(mockPerformanceTimer.mark).toHaveBeenCalledWith(
    // Since this query does not have a displayName, the query index is used instead. That's why it says '2' here.
    `${_performanceMarks.PERFORMANCE_MARKS.reactQueryUseQuery}.2`, 'start');

    // Verify all timers are closed regardless of fetch status
    expect(mockPerformanceTimer.mark).toHaveBeenCalledWith(`${_performanceMarks.PERFORMANCE_MARKS.reactQueryUseQuery}.SuccessQuery-0`, 'end', expect.objectContaining({
      detail: 'success-hash'
    }));
    expect(mockPerformanceTimer.mark).toHaveBeenCalledWith(`${_performanceMarks.PERFORMANCE_MARKS.reactQueryUseQuery}.ErrorQuery-1`, 'end', expect.objectContaining({
      detail: 'error-hash'
    }));
    expect(mockPerformanceTimer.mark).toHaveBeenCalledWith(
    // Since this query does not have a displayName, the query index is used instead. That's why it says '2' here.
    `${_performanceMarks.PERFORMANCE_MARKS.reactQueryUseQuery}.2`, 'end', expect.objectContaining({
      detail: 'another-success-hash'
    }));

    // Verify only the failing query is logged
    expect(_loggerInstance.default.error).toHaveBeenCalledTimes(1);
    expect(_loggerInstance.default.error).toHaveBeenCalledWith('Query during SSR results in an error', {
      namespace: 'with-react-query.doInitAppState',
      additionalProperties: {
        queryHash: 'error-hash',
        error: mockError
      }
    });
  }));
  describe('per-query child spans (res.locals.__withChildSpan)', () => {
    const makeRes = (queries, withChildSpan) => ({
      locals: _objectSpread({
        __queryClient: {
          getQueryCache: () => ({
            getAll: () => queries
          })
        }
      }, withChildSpan ? {
        __withChildSpan: withChildSpan
      } : {}),
      __performanceTimer: {
        mark: jest.fn()
      }
    });
    test('wraps each query fetch in an injected child span named for the query', /*#__PURE__*/_asyncToGenerator(function* () {
      const spanNames = [];
      // Faithful stand-in for the real withChildSpan: runs fn() within the span.
      const withChildSpan = (name, fn) => {
        spanNames.push(name);
        return fn();
      };
      const fetchA = jest.fn().mockResolvedValue({
        data: 'a'
      });
      const fetchB = jest.fn().mockResolvedValue({
        data: 'b'
      });
      const queries = [{
        options: {
          enabled: true
        },
        meta: {
          displayName: 'useProductSearch'
        },
        queryHash: 'h-a',
        fetch: fetchA
      }, {
        options: {
          enabled: true
        },
        meta: {
          displayName: 'useCategory'
        },
        queryHash: 'h-b',
        fetch: fetchB
      }];
      const res = makeRes(queries, withChildSpan);
      const Component = (0, _index.withReactQuery)({});
      yield Component.doInitAppState({
        res,
        appJSX: /*#__PURE__*/_react2.default.createElement("div", null, "Test")
      });

      // Each query fetch ran inside an injected child span.
      expect(fetchA).toHaveBeenCalledTimes(1);
      expect(fetchB).toHaveBeenCalledTimes(1);
      expect(spanNames).toContain('scapi:useProductSearch');
      expect(spanNames).toContain('scapi:useCategory');
    }));
    test('falls back to the query index when no displayName is present', /*#__PURE__*/_asyncToGenerator(function* () {
      const spanNames = [];
      const withChildSpan = (name, fn) => {
        spanNames.push(name);
        return fn();
      };
      const queries = [{
        options: {
          enabled: true
        },
        meta: {},
        queryHash: 'h-0',
        fetch: jest.fn().mockResolvedValue({})
      }];
      const res = makeRes(queries, withChildSpan);
      const Component = (0, _index.withReactQuery)({});
      yield Component.doInitAppState({
        res,
        appJSX: /*#__PURE__*/_react2.default.createElement("div", null, "Test")
      });
      expect(spanNames).toContain('scapi:0');
    }));
    test('no-op when __withChildSpan is absent: queries still fetch (no throw)', /*#__PURE__*/_asyncToGenerator(function* () {
      const fetchA = jest.fn().mockResolvedValue({
        data: 'a'
      });
      const queries = [{
        options: {
          enabled: true
        },
        meta: {
          displayName: 'useX'
        },
        queryHash: 'h-a',
        fetch: fetchA
      }];
      const res = makeRes(queries, undefined);
      const Component = (0, _index.withReactQuery)({});
      yield expect(Component.doInitAppState({
        res,
        appJSX: /*#__PURE__*/_react2.default.createElement("div", null, "Test")
      })).resolves.toBeDefined();
      expect(fetchA).toHaveBeenCalledTimes(1);
    }));
  });
});