"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.withServerSpan = exports.withChildSpan = exports.setActiveSpanAttribute = exports.isDistributedTracingEnabled = exports.getCurrentTraceparent = exports.extractContext = void 0;
var _sdkTraceNode = require("@opentelemetry/sdk-trace-node");
var _sdkTraceBase = require("@opentelemetry/sdk-trace-base");
var _resources = require("@opentelemetry/resources");
var _api = require("@opentelemetry/api");
var _core = require("@opentelemetry/core");
var _contextAsyncHooks = require("@opentelemetry/context-async-hooks");
var _mrtConsoleSpanExporter = require("./mrt-console-span-exporter");
var _opentelemetryConfig = require("../../utils/opentelemetry-config");
var _loggerInstance = _interopRequireDefault(require("../../utils/logger-instance"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; } /*
 * Copyright (c) 2026, Salesforce, Inc.
 * All rights reserved.
 * SPDX-License-Identifier: BSD-3-Clause
 * For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/BSD-3-Clause
 */ /**
 * Distributed tracing for PWA Kit.
 *
 * Self-contained OpenTelemetry setup that runs alongside the existing
 * `server_timing` tracing without disturbing it:
 *   - Own NodeTracerProvider + SimpleSpanProcessor(MrtConsoleSpanExporter).
 *   - Tracer obtained via provider.getTracer() (NOT the global trace.getTracer()).
 *   - W3CTraceContextPropagator instance used directly for extract/inject.
 *   - NEVER calls provider.register() or propagation.setGlobalPropagator(),
 *     so the existing B3 global propagator and server_timing path are untouched.
 *   - Installs a global AsyncHooksContextManager (once, lazily) so spans
 *     propagate across async boundaries.
 *
 * Gated on getOTELConfig().enabled. Extract-only: a request without an incoming
 * traceparent produces no DT span (no fresh-root minting).
 */
const SERVICE_NAME = 'pwa-kit-react-sdk';
let tracer = null;
let contextManagerInitialized = false;
const propagator = new _core.W3CTraceContextPropagator();
const isDistributedTracingEnabled = () => (0, _opentelemetryConfig.getOTELConfig)().enabled;
exports.isDistributedTracingEnabled = isDistributedTracingEnabled;
const getTracer = () => {
  if (tracer) return tracer;

  // Set up AsyncHooksContextManager for context propagation across async boundaries
  if (!contextManagerInitialized) {
    const contextManager = new _contextAsyncHooks.AsyncHooksContextManager();
    contextManager.enable();
    _api.context.setGlobalContextManager(contextManager);
    contextManagerInitialized = true;
  }

  // Resource attributes sourced from the MRT-provided runtime env. BUNDLE_ID
  // is a deploy identifier (numeric), not a semver — MRT exposes no semver var.
  const resource = (0, _resources.resourceFromAttributes)({
    'service.name': SERVICE_NAME,
    'service.namespace': process.env.MOBIFY_PROPERTY_ID || '',
    'service.version': process.env.BUNDLE_ID || '',
    'service.instance.id': process.env.AWS_LAMBDA_FUNCTION_NAME || '',
    'deployment.environment': process.env.DEPLOY_TARGET || ''
  });
  const provider = new _sdkTraceNode.NodeTracerProvider({
    resource,
    spanProcessors: [new _sdkTraceBase.SimpleSpanProcessor(new _mrtConsoleSpanExporter.MrtConsoleSpanExporter())]
  });
  tracer = provider.getTracer(SERVICE_NAME);
  return tracer;
};
const extractContext = headers => {
  try {
    return propagator.extract(_api.context.active(), headers || {}, {
      get: (carrier, key) => carrier[key],
      // W3CTraceContextPropagator.extract only reads via get(), never keys() —
      // this is required by the TextMapGetter shape but unreachable in practice.
      /* istanbul ignore next */
      keys: carrier => Object.keys(carrier)
    });
  } catch (error) {
    _loggerInstance.default.warn('DT extract failed', {
      namespace: 'distributed-tracing.extractContext',
      additionalProperties: {
        error: error.message
      }
    });
    return _api.context.active();
  }
};
exports.extractContext = extractContext;
const withServerSpan = exports.withServerSpan = /*#__PURE__*/function () {
  var _ref = _asyncToGenerator(function* (req, res, parentCtx, fn) {
    var _trace$getSpan, _req$headers, _res$locals, _res$locals$site, _res$locals2, _res$locals3, _res$locals4;
    if (!isDistributedTracingEnabled()) return fn();

    // Extract-only: if the incoming request carried no valid W3C traceparent,
    // the extracted context has no active span context. Do not mint a fresh root
    // span — just run fn(). eCDN is the trusted trace generator in production.
    const incoming = (_trace$getSpan = _api.trace.getSpan(parentCtx)) === null || _trace$getSpan === void 0 ? void 0 : _trace$getSpan.spanContext();
    if (!incoming || !incoming.traceId) return fn();
    const t = getTracer();
    const method = req.method || 'GET';
    const urlPath = (req.originalUrl || req.url || '/').split('?')[0];
    const serverAddress = ((_req$headers = req.headers) === null || _req$headers === void 0 ? void 0 : _req$headers.host) || '';
    const attributes = {
      'http.request.method': method,
      'url.path': urlPath
    };
    if (serverAddress) {
      attributes['server.address'] = serverAddress;
    }

    // Custom attributes from res.locals
    if ((_res$locals = res.locals) !== null && _res$locals !== void 0 && (_res$locals$site = _res$locals.site) !== null && _res$locals$site !== void 0 && _res$locals$site.id) {
      attributes['site_name'] = res.locals.site.id;
    }
    if ((_res$locals2 = res.locals) !== null && _res$locals2 !== void 0 && _res$locals2.clientId) {
      attributes['client_id'] = res.locals.clientId;
    }
    if ((_res$locals3 = res.locals) !== null && _res$locals3 !== void 0 && _res$locals3.realm) {
      attributes['realm'] = res.locals.realm;
    }
    if ((_res$locals4 = res.locals) !== null && _res$locals4 !== void 0 && _res$locals4.instanceType) {
      attributes['instance_type'] = res.locals.instanceType;
    }

    // Name on method only; the concrete path stays in url.path and the route
    // template in http.route, so the span name has low cardinality.
    const span = t.startSpan(`ssr.render ${method}`, {
      attributes
    }, parentCtx);

    // Set http.response.status_code on response finish (once-guard)
    let statusCaptured = false;
    if (res && typeof res.on === 'function') {
      res.on('finish', () => {
        if (!statusCaptured) {
          statusCaptured = true;
          try {
            span.setAttribute('http.response.status_code', res.statusCode);
          } catch {
            // non-essential
          }
        }
      });
    }
    return _api.context.with(_api.trace.setSpan(parentCtx, span), /*#__PURE__*/_asyncToGenerator(function* () {
      try {
        const sc = span.spanContext();
        if (sc) {
          const flags = sc.traceFlags.toString(16).padStart(2, '0');
          const traceparent = `00-${sc.traceId}-${sc.spanId}-${flags}`;
          // Expose the traceparent as a plain string for outbound propagation:
          // the template reads res.locals.traceparent and forwards it on the
          // CommerceApiProvider `headers` prop. Set inside the active span so it
          // carries this request's trace id.
          if (res !== null && res !== void 0 && res.locals) {
            res.locals.traceparent = traceparent;
          }
          if (res && typeof res.setHeader === 'function') {
            res.setHeader('traceparent', traceparent);
          }
        }
      } catch {
        // traceparent exposure is non-essential
      }
      try {
        return yield fn();
      } catch (error) {
        span.setStatus({
          code: _api.SpanStatusCode.ERROR,
          message: error.message
        });
        throw error;
      } finally {
        // Fallback: capture the final status code in case the response
        // 'finish' event never fired (e.g. error path). The once-guard on
        // the finish handler makes a double-set harmless.
        try {
          if (res !== null && res !== void 0 && res.statusCode) {
            span.setAttribute('http.response.status_code', res.statusCode);
          }
        } catch {
          // non-essential
        }
        span.end();
      }
    }));
  });
  return function withServerSpan(_x, _x2, _x3, _x4) {
    return _ref.apply(this, arguments);
  };
}();
const withChildSpan = exports.withChildSpan = /*#__PURE__*/function () {
  var _ref3 = _asyncToGenerator(function* (name, fn) {
    if (!isDistributedTracingEnabled()) return fn();
    const t = getTracer();
    const span = t.startSpan(name, undefined, _api.context.active());
    try {
      return yield _api.context.with(_api.trace.setSpan(_api.context.active(), span), /*#__PURE__*/_asyncToGenerator(function* () {
        return yield fn(span);
      }));
    } catch (error) {
      span.setStatus({
        code: _api.SpanStatusCode.ERROR,
        message: error.message
      });
      throw error;
    } finally {
      span.end();
    }
  });
  return function withChildSpan(_x5, _x6) {
    return _ref3.apply(this, arguments);
  };
}();

/**
 * Sets an attribute on the currently-active span, if any. Used for attributes
 * that are only known partway through the render (e.g. `http.route`, which is
 * resolved after route matching, inside the server span). No-op when tracing is
 * disabled or there is no active span.
 *
 * @param {string} key
 * @param {string|number|boolean} value
 */
const setActiveSpanAttribute = (key, value) => {
  if (!isDistributedTracingEnabled()) return;
  try {
    const span = _api.trace.getSpan(_api.context.active());
    if (span && value != null && value !== '') {
      span.setAttribute(key, value);
    }
  } catch {
    // attributes are non-essential — never break request handling
  }
};

/**
 * Returns the W3C `traceparent` string for the currently-active span, or null
 * if tracing is disabled or there is no active span. Must be called from within
 * the active server-span context (e.g. during SSR inside withServerSpan).
 *
 * Used for outbound propagation: the template passes this as a callable header
 * value so each SCAPI request carries the current span's traceparent.
 *
 * @returns {string|null} e.g. "00-<traceId>-<spanId>-01"
 */
exports.setActiveSpanAttribute = setActiveSpanAttribute;
const getCurrentTraceparent = () => {
  if (!isDistributedTracingEnabled()) return null;
  try {
    var _trace$getSpan2;
    const sc = (_trace$getSpan2 = _api.trace.getSpan(_api.context.active())) === null || _trace$getSpan2 === void 0 ? void 0 : _trace$getSpan2.spanContext();
    if (!sc || !sc.traceId) return null;
    const flags = sc.traceFlags.toString(16).padStart(2, '0');
    return `00-${sc.traceId}-${sc.spanId}-${flags}`;
  } catch {
    return null;
  }
};
exports.getCurrentTraceparent = getCurrentTraceparent;