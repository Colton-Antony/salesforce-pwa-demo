"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.tracePerformance = exports.shutdownServerTracing = exports.isServerTracingInitialized = exports.initializeServerTracing = exports.getServerTracingProvider = void 0;
var _sdkTraceNode = require("@opentelemetry/sdk-trace-node");
var _propagatorB = require("@opentelemetry/propagator-b3");
var _resources = require("@opentelemetry/resources");
var _api = require("@opentelemetry/api");
var _loggerInstance = _interopRequireDefault(require("../../utils/logger-instance"));
var _opentelemetryConfig = require("../../utils/opentelemetry-config");
var _opentelemetry = require("../../utils/opentelemetry");
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; } /*
 * Copyright (c) 2025, Salesforce, Inc.
 * All rights reserved.
 * SPDX-License-Identifier: BSD-3-Clause
 * For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/BSD-3-Clause
 */
let provider = null;

/**
 * Initialize OpenTelemetry tracing for server-side rendering
 * @param {Object} options
 * @param {string} [options.serviceName]
 * @param {string} [options.serviceVersion]
 * @param {boolean} [options.enabled]
 * @returns {NodeTracerProvider|null}
 */
const initializeServerTracing = (options = {}) => {
  const {
    serviceName = options.serviceName || (0, _opentelemetryConfig.getServiceName)(),
    serviceVersion,
    enabled = (0, _opentelemetryConfig.getOTELConfig)().enabled
  } = options;
  // If tracing is disabled, return null without initializing
  if (!enabled) {
    return null;
  }
  try {
    // Build resource attributes
    const resourceAttributes = {
      'service.name': serviceName
    };

    // Add service version if provided
    if (serviceVersion) {
      resourceAttributes['service.version'] = serviceVersion;
    }

    // Initialize the tracer provider
    provider = new _sdkTraceNode.NodeTracerProvider({
      resource: (0, _resources.resourceFromAttributes)(resourceAttributes)
    });

    // Add B3 propagator
    _api.propagation.setGlobalPropagator(new _propagatorB.B3Propagator());
    provider.register();
    return provider;
  } catch (error) {
    // Log errors from OpenTelemetry initialization
    _loggerInstance.default.error('Failed to initialize OpenTelemetry provider', {
      namespace: 'opentelemetry-server.initializeServerTracing',
      additionalProperties: {
        error: error.message
      }
    });
    return null;
  }
};

/**
 * Shutdown OpenTelemetry tracing and clean up resources
 * @returns {Promise<void>}
 */
exports.initializeServerTracing = initializeServerTracing;
const shutdownServerTracing = exports.shutdownServerTracing = /*#__PURE__*/function () {
  var _ref = _asyncToGenerator(function* () {
    if (provider) {
      try {
        yield provider.shutdown();
        provider = null; // Clean up after successful shutdown
      } catch (error) {
        _loggerInstance.default.warn('Failed to shutdown OpenTelemetry provider', {
          namespace: 'opentelemetry-server.shutdownServerTracing',
          additionalProperties: {
            error: error.message
          }
        });
      }
    }
  });
  return function shutdownServerTracing() {
    return _ref.apply(this, arguments);
  };
}();

/**
 * Get the current OpenTelemetry provider instance
 * @returns {NodeTracerProvider|null} The current provider or null if not initialized
 */
const getServerTracingProvider = () => {
  return provider;
};

/**
 * Check if OpenTelemetry tracing is currently initialized
 * @returns {boolean} True if tracing is initialized, false otherwise
 */
exports.getServerTracingProvider = getServerTracingProvider;
const isServerTracingInitialized = () => {
  return provider !== null;
};

/**
 * Creates a span for performance measurement
 * @param {string} name - The name of the performance span
 * @param {Function} fn - The function to measure
 * @param {Object} res - The response object (optional)
 * @returns {Promise<any>} The result of the function
 */
exports.isServerTracingInitialized = isServerTracingInitialized;
const tracePerformance = exports.tracePerformance = /*#__PURE__*/function () {
  var _ref2 = _asyncToGenerator(function* (name, fn, res = null, req = null) {
    // Check if OpenTelemetry is properly configured
    const otelConfig = (0, _opentelemetryConfig.getOTELConfig)();
    let rootSpan = null;
    let ctx = null;
    const includeServerTimingHeader = '__server_timing' in req.query;
    const shouldTrackPerformance = includeServerTimingHeader || process.env.SERVER_TIMING;
    if (otelConfig.enabled && shouldTrackPerformance) {
      // Initialize server tracing if needed for this request
      if (shouldTrackPerformance && !isServerTracingInitialized()) {
        initializeServerTracing();
      }
      const tracer = _api.trace.getTracer((0, _opentelemetryConfig.getServiceName)());
      // Create the root span
      rootSpan = tracer.startSpan(name, {
        attributes: {
          'service.name': (0, _opentelemetryConfig.getServiceName)()
        }
      });

      // Create a new context with the root span
      ctx = _api.trace.setSpan(_api.context.active(), rootSpan);

      // Inject B3 headers into response if available
      if (res && (0, _opentelemetryConfig.getOTELConfig)().enabled && shouldTrackPerformance) {
        res.setHeader('x-b3-traceid', rootSpan.spanContext().traceId);
        res.setHeader('x-b3-spanid', rootSpan.spanContext().spanId);
        res.setHeader('x-b3-sampled', '1');
      }
    }
    try {
      // Run the function within the context of the root span (if ctx exists)
      const result = ctx ? yield _api.context.with(ctx, /*#__PURE__*/_asyncToGenerator(function* () {
        try {
          return yield fn();
        } catch (error) {
          rootSpan.setStatus({
            code: _api.SpanStatusCode.ERROR,
            message: error.message
          });
          throw error;
        }
      })) : yield fn();
      if (otelConfig.enabled && shouldTrackPerformance) {
        rootSpan.end();
        (0, _opentelemetry.logSpanData)(rootSpan, 'end', res);
      }
      return result;
    } catch (error) {
      if (otelConfig.enabled && shouldTrackPerformance) {
        rootSpan.end();
        (0, _opentelemetry.logSpanData)(rootSpan, 'end', res);
      }
      throw error;
    }
  });
  return function tracePerformance(_x, _x2) {
    return _ref2.apply(this, arguments);
  };
}();