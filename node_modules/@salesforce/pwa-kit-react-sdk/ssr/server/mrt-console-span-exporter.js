"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.MrtConsoleSpanExporter = void 0;
var _sdkTraceBase = require("@opentelemetry/sdk-trace-base");
var _core = require("@opentelemetry/core");
var _opentelemetryConfig = require("../../utils/opentelemetry-config");
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); } /*
 * Copyright (c) 2026, Salesforce, Inc.
 * All rights reserved.
 * SPDX-License-Identifier: BSD-3-Clause
 * For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/BSD-3-Clause
 */ /**
 * MRT-compatible console span exporter.
 *
 * MRT scrapes stdout and forwards spans to the trace backend (Jaeger); this
 * exporter's only job is to print the JSON shape MRT expects. No OTLP endpoint
 * is involved.
 *
 * @env OTEL_TRACING_ENABLED — resolved via getOTELConfig().enabled
 */
class MrtConsoleSpanExporter extends _sdkTraceBase.ConsoleSpanExporter {
  export(spans, resultCallback) {
    for (const span of spans) {
      try {
        var _span$parentSpanConte, _span$resource;
        const ctx = span.spanContext();
        const spanData = {
          traceId: ctx.traceId,
          parentId: (_span$parentSpanConte = span.parentSpanContext) === null || _span$parentSpanConte === void 0 ? void 0 : _span$parentSpanConte.spanId,
          name: span.name,
          id: ctx.spanId,
          kind: span.kind,
          timestamp: (0, _core.hrTimeToTimeStamp)(span.startTime),
          duration: span.duration,
          // Merge resource attributes (e.g. service.name, set on the
          // provider Resource) into span attributes. MRT/Jaeger keys on
          // service.name in attributes, matching server_timing's logSpanData.
          attributes: _objectSpread(_objectSpread({}, (_span$resource = span.resource) === null || _span$resource === void 0 ? void 0 : _span$resource.attributes), span.attributes),
          status: span.status,
          events: span.events,
          links: span.links,
          start_time: span.startTime,
          end_time: span.endTime,
          forwardTrace: (0, _opentelemetryConfig.getOTELConfig)().enabled
        };
        // eslint-disable-next-line no-console -- intentional: MRT collects stdout as the telemetry transport
        console.info(JSON.stringify(spanData));
      } catch {
        // Skip malformed spans — never let a serialization failure propagate
      }
    }
    resultCallback({
      code: _core.ExportResultCode.SUCCESS
    });
  }
}
exports.MrtConsoleSpanExporter = MrtConsoleSpanExporter;