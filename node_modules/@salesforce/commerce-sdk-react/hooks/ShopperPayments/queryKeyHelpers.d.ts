import { ShopperPayments } from 'commerce-sdk-isomorphic';
import { Argument, ExcludeTail } from '../types';
type Client = ShopperPayments<{
    shortCode: string;
}>;
type Params<T extends keyof QueryKeys> = Partial<Argument<Client[T]>['parameters']>;
export type QueryKeys = {
    getPaymentConfiguration: [
        '/commerce-sdk-react',
        '/organizations/',
        string | undefined,
        '/payments/',
        Params<'getPaymentConfiguration'>
    ];
};
type QueryKeyHelper<T extends keyof QueryKeys> = {
    /** Generates the path component of the query key for an endpoint. */
    path: (params: Params<T>) => ExcludeTail<QueryKeys[T]>;
    /** Generates the complete query key for an endpoint. */
    queryKey: (params: Params<T>) => QueryKeys[T];
};
export declare const getPaymentConfiguration: QueryKeyHelper<'getPaymentConfiguration'>;
export {};
//# sourceMappingURL=queryKeyHelpers.d.ts.map