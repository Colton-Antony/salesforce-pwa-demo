"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getPaymentConfiguration = void 0;
var _commerceSdkIsomorphic = require("commerce-sdk-isomorphic");
var _utils = require("../utils");
/*
 * Copyright (c) 2025, Salesforce, Inc.
 * All rights reserved.
 * SPDX-License-Identifier: BSD-3-Clause
 * For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/BSD-3-Clause
 */

// We must use a client with no parameters in order to have required/optional match the API spec

// This is defined here, rather than `types.ts`, because it relies on `Client` and `QueryKeys`,
// and making those generic would add too much complexity.

const getPaymentConfiguration = exports.getPaymentConfiguration = {
  path: params => ['/commerce-sdk-react', '/organizations/', params === null || params === void 0 ? void 0 : params.organizationId, '/payments/'],
  queryKey: params => {
    return [...getPaymentConfiguration.path(params),
    // pickValidParams returns the filtered parameters, TypeScript sees that zoneId
    // could be string | null, and complains because null isn't allowed in the query key.
    // omitNullable removes null values from the parameters (zoneId is optional but NOT nullable)
    (0, _utils.omitNullable)((0, _utils.pickValidParams)(params || {}, _commerceSdkIsomorphic.ShopperPayments.paramKeys.getPaymentConfiguration))];
  }
};