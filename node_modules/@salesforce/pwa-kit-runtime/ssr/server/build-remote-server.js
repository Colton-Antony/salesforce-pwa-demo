"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.setTokensInLogoutRequest = exports.setRefreshTokenHeader = exports.once = exports.isContentTypeBinary = exports.isBinary = exports.SlasProxyAuthType = exports.SLAS_PRIVATE_PROXY_ALLOWLIST = exports.RemoteServerFactory = exports.RefreshTokenNotFoundError = exports.REMOTE_REQUIRED_ENV_VARS = exports.HttpMethod = void 0;
var _path = _interopRequireWildcard(require("path"));
var _cookie = _interopRequireDefault(require("cookie"));
var _constants = require("./constants");
var _httponlyCookieConfig = require("./httponly-cookie-config");
var _previewContext = require("./preview-context");
var _ssrServer = require("../../utils/ssr-server");
var _dns = _interopRequireDefault(require("dns"));
var _express = _interopRequireDefault(require("express"));
var _ssrCache = require("../../utils/ssr-cache");
var _mergeDescriptors = _interopRequireDefault(require("merge-descriptors"));
var _ssrProxying = require("../../utils/ssr-proxying");
var _assert = _interopRequireDefault(require("assert"));
var _semver = _interopRequireDefault(require("semver"));
var _package = _interopRequireDefault(require("../../package.json"));
var _fs = _interopRequireDefault(require("fs"));
var _express2 = require("./express");
var _http = _interopRequireDefault(require("http"));
var _https = _interopRequireDefault(require("https"));
var _ssrShared = require("../../utils/ssr-shared");
var _ssrNamespacePaths = require("../../utils/ssr-namespace-paths");
var _configureProxy = require("../../utils/ssr-server/configure-proxy");
var _morgan = _interopRequireDefault(require("morgan"));
var _loggerInstance = _interopRequireDefault(require("../../utils/logger-instance"));
var _httpProxyMiddleware = require("http-proxy-middleware");
var _hybridProxy = require("../../utils/ssr-server/hybrid-proxy");
var _convertExpressRoute = require("../../utils/ssr-server/convert-express-route");
var _ssrConfig = require("../../utils/ssr-config");
var _serverlessAdapter = require("@h4ad/serverless-adapter");
var _default = require("@h4ad/serverless-adapter/lib/handlers/default");
var _callback = require("@h4ad/serverless-adapter/lib/resolvers/callback");
var _aws = require("@h4ad/serverless-adapter/lib/adapters/aws");
var _express3 = require("@h4ad/serverless-adapter/lib/frameworks/express");
var _typeIs = require("type-is");
var _processTokenResponse = require("./process-token-response");
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function _interopRequireWildcard(e, t) { if ("function" == typeof WeakMap) var r = new WeakMap(), n = new WeakMap(); return (_interopRequireWildcard = function (e, t) { if (!t && e && e.__esModule) return e; var o, i, f = { __proto__: null, default: e }; if (null === e || "object" != typeof e && "function" != typeof e) return f; if (o = t ? n : r) { if (o.has(e)) return o.get(e); o.set(e, f); } for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]); return f; })(e, t); }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); } /*
 * Copyright (c) 2022, Salesforce, Inc.
 * All rights reserved.
 * SPDX-License-Identifier: BSD-3-Clause
 * For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/BSD-3-Clause
 */
const {
  refreshTokenRegistered,
  refreshTokenGuest,
  accessToken: accessTokenConfig
} = _httponlyCookieConfig.SESSION_COOKIE_CONFIG;
/**
 * An Array of mime-types (Content-Type values) that are considered
 * as binary by serverless-adapter when processing responses.
 * We intentionally exclude all text/* values since we assume UTF8
 * encoding and there's no reason to bulk up the response by base64
 * encoding the result.
 *
 * We can use '*' in these types as a wildcard - see
 * https://www.npmjs.com/package/type-is#type--typeisismediatype-types
 * @private
 */
const binaryMimeTypes = ['application/*', 'audio/*', 'font/*', 'image/*', 'video/*'];
const isContentTypeBinary = headers => {
  // Replicating the aws-serverless-express behavior
  let contentType = headers['content-type'] || headers['Content-Type'];
  if (!contentType) {
    return false;
  }
  // Remove the encoding from the content type
  contentType = contentType.split(';')[0];
  return !!(0, _typeIs.is)(contentType, binaryMimeTypes);
};
exports.isContentTypeBinary = isContentTypeBinary;
const isBinary = headers => {
  return isContentTypeBinary(headers);
};

/**
 * Error thrown when the refresh token HttpOnly cookie is not found on the incoming request.
 * Handled specifically in the SLAS proxy to return a 401 instead of forwarding to SLAS.
 */
exports.isBinary = isBinary;
class RefreshTokenNotFoundError extends Error {
  constructor(message) {
    super(message);
    this.name = 'RefreshTokenNotFoundError';
  }
}

/**
 * Inject the refresh token from HttpOnly cookies as the sfdc_refresh_token header.
 * SLAS uses sfdc_refresh_token as a fallback when the refresh_token body parameter is
 * missing or empty (which is the case when HttpOnly session cookies are enabled, because
 * client-side JavaScript cannot read the HttpOnly refresh token cookie).
 * @throws {RefreshTokenNotFoundError} If the refresh token cookie is missing.
 * @private
 */
exports.RefreshTokenNotFoundError = RefreshTokenNotFoundError;
const setRefreshTokenHeader = (proxyRequest, incomingRequest) => {
  const cookieHeader = incomingRequest.headers.cookie;
  if (!cookieHeader) {
    throw new RefreshTokenNotFoundError('No cookies present on request. Cannot inject refresh token.');
  }
  const cookies = _cookie.default.parse(cookieHeader);
  const siteId = (0, _httponlyCookieConfig.getSiteId)(incomingRequest);
  if (!siteId) {
    throw new RefreshTokenNotFoundError('x-site-id header is missing on SLAS token request. ' + 'Cannot inject refresh token. ' + 'Ensure the x-site-id header is set in CommerceApiProvider headers.');
  }

  // Try registered refresh token first, then guest
  const refreshToken = cookies[(0, _httponlyCookieConfig.getCookieName)(refreshTokenRegistered, siteId)] || cookies[(0, _httponlyCookieConfig.getCookieName)(refreshTokenGuest, siteId)];
  if (!refreshToken) {
    throw new RefreshTokenNotFoundError('Refresh token cookie not found. Cannot proceed with refresh token flow.');
  }
  proxyRequest.setHeader('sfdc_refresh_token', refreshToken);
};

/**
 * Inject Bearer token and refresh token from HttpOnly cookies for the SLAS logout endpoint.
 * Reads the access token and refresh token from cookies keyed by siteId (from the x-site-id header),
 * sets the Authorization header, and appends refresh_token to the query string.
 * @private
 */
exports.setRefreshTokenHeader = setRefreshTokenHeader;
const setTokensInLogoutRequest = (proxyRequest, incomingRequest) => {
  const cookieHeader = incomingRequest.headers.cookie;
  if (!cookieHeader) return;
  const cookies = _cookie.default.parse(cookieHeader);
  const siteId = (0, _httponlyCookieConfig.getSiteId)(incomingRequest);
  if (!siteId) {
    _loggerInstance.default.warn('x-site-id header is missing on SLAS logout request. ' + 'Token injection skipped. ' + 'Ensure the x-site-id header is set in CommerceApiProvider headers.', {
      namespace: 'setTokensInLogoutRequest'
    });
    return;
  }

  // Inject Bearer token from access token cookie
  const accessToken = cookies[(0, _httponlyCookieConfig.getCookieName)(accessTokenConfig, siteId)];
  if (accessToken) {
    proxyRequest.setHeader('Authorization', `Bearer ${accessToken}`);
  }

  // Inject refresh_token into query string from HttpOnly cookie
  const refreshToken = cookies[(0, _httponlyCookieConfig.getCookieName)(refreshTokenRegistered, siteId)];
  if (refreshToken) {
    const separator = proxyRequest.path.includes('?') ? '&' : '?';
    proxyRequest.path += `${separator}refresh_token=${encodeURIComponent(refreshToken)}`;
  } else {
    _loggerInstance.default.warn(`Refresh token cookie (${(0, _httponlyCookieConfig.getCookieName)(refreshTokenRegistered, siteId)}) not found for ${incomingRequest.path}. The logout request may fail.`, {
      namespace: 'setTokensInLogoutRequest'
    });
  }
};

/**
 * Handle all HttpOnly session cookie logic for the outgoing SLAS proxy request.
 * Consolidates refresh token injection, logout token injection, session cookie
 * stripping, and internal header cleanup.
 * @throws {RefreshTokenNotFoundError} if the refresh token cookie is missing on a refresh_token grant request.
 * @private
 */
exports.setTokensInLogoutRequest = setTokensInLogoutRequest;
const handleHttpOnlyCookiesOnProxyReq = (proxyRequest, incomingRequest) => {
  var _incomingRequest$path;
  if (incomingRequest.headers[_constants.X_GRANT_TYPE] === 'refresh_token') {
    setRefreshTokenHeader(proxyRequest, incomingRequest);
    proxyRequest.removeHeader(_constants.X_GRANT_TYPE);
  }
  if ((_incomingRequest$path = incomingRequest.path) !== null && _incomingRequest$path !== void 0 && _incomingRequest$path.match(_constants.SLAS_LOGOUT_ENDPOINT)) {
    setTokensInLogoutRequest(proxyRequest, incomingRequest);
  }
  (0, _configureProxy.stripSessionCookies)(proxyRequest, incomingRequest);
  proxyRequest.removeHeader(_constants.X_SITE_ID);
};

/**
 * Handle all HttpOnly session cookie logic for the SLAS proxy response.
 * Sets HttpOnly cookies on token responses and expires them on logout.
 * Throws if setHttpOnlySessionCookies fails — caller is responsible for error response.
 * @private
 */
const handleHttpOnlyCookiesOnProxyRes = (workingBuffer, proxyRes, req, res, options, logNamespace) => {
  var _req$path, _req$path2;
  const isTokenEndpoint = (_req$path = req.path) === null || _req$path === void 0 ? void 0 : _req$path.match(options.tokenResponseEndpoints);
  if (proxyRes.statusCode === 200 && isTokenEndpoint) {
    workingBuffer = (0, _processTokenResponse.setHttpOnlySessionCookies)(workingBuffer, proxyRes, req, res, options);
  }
  if ((_req$path2 = req.path) !== null && _req$path2 !== void 0 && _req$path2.match(_constants.SLAS_LOGOUT_ENDPOINT)) {
    try {
      (0, _processTokenResponse.expireHttpOnlySessionCookies)(req, res, options);
    } catch (error) {
      _loggerInstance.default.warn('Error expiring HttpOnly session cookies on logout', {
        namespace: logNamespace,
        additionalProperties: {
          error: error.message || error
        }
      });
    }
  }
  return workingBuffer;
};

/**
 * Environment variables that must be set for the Express app to run remotely.
 *
 * @private
 */
const REMOTE_REQUIRED_ENV_VARS = exports.REMOTE_REQUIRED_ENV_VARS = ['BUNDLE_ID', 'DEPLOY_TARGET', 'EXTERNAL_DOMAIN_NAME', 'MOBIFY_PROPERTY_ID'];

/**
 * Uppercase HTTP method names.
 */
const HttpMethod = exports.HttpMethod = Object.freeze({
  GET: 'GET',
  POST: 'POST'
});

/**
 * Credential injection modes used by the SLAS private-client proxy allow-list.
 *
 *  - `BASIC`       attaches `Authorization: Basic <private-client-creds>`
 *  - `SFDC_HEADER` attaches `_sfdc_client_auth: <private-client-creds>`
 *  - `NONE`        forwards the request without credential injection
 */
const SlasProxyAuthType = exports.SlasProxyAuthType = Object.freeze({
  BASIC: 'basic',
  SFDC_HEADER: 'sfdc-header',
  NONE: 'none'
});

/**
 * Endpoints the SLAS private-client proxy will forward, and the credential
 * header (if any) the proxy attaches.
 *
 * Each entry is matched as an exact suffix of decoded path segments after the
 * `/shopper/auth/{version}/organizations/{orgId}/` prefix. Methods are taken
 * from the SLAS OpenAPI spec.
 *
 * Projects can override this via `options.slasPrivateClientAllowList` in
 * `ssr.js`. Use `HttpMethod` and `SlasProxyAuthType` for entries.
 *
 * TODO: derive this list from the SLAS OpenAPI spec so it stays in sync
 *       automatically.
 */
const SLAS_PRIVATE_PROXY_ALLOWLIST = exports.SLAS_PRIVATE_PROXY_ALLOWLIST = Object.freeze([{
  segments: ['oauth2', 'token'],
  methods: [HttpMethod.POST],
  injectAuth: SlasProxyAuthType.BASIC
}, {
  segments: ['oauth2', 'logout'],
  methods: [HttpMethod.GET, HttpMethod.POST],
  injectAuth: SlasProxyAuthType.NONE
}, {
  segments: ['oauth2', 'login'],
  methods: [HttpMethod.POST],
  injectAuth: SlasProxyAuthType.NONE
}, {
  segments: ['oauth2', 'passwordless', 'login'],
  methods: [HttpMethod.POST],
  injectAuth: SlasProxyAuthType.BASIC
}, {
  segments: ['oauth2', 'passwordless', 'token'],
  methods: [HttpMethod.POST],
  injectAuth: SlasProxyAuthType.BASIC
}, {
  segments: ['oauth2', 'password', 'reset'],
  methods: [HttpMethod.POST],
  injectAuth: SlasProxyAuthType.BASIC
}, {
  segments: ['oauth2', 'password', 'action'],
  methods: [HttpMethod.POST],
  injectAuth: SlasProxyAuthType.BASIC
}, {
  segments: ['oauth2', 'trusted-agent', 'authorize'],
  methods: [HttpMethod.GET],
  injectAuth: SlasProxyAuthType.NONE
}, {
  segments: ['oauth2', 'trusted-agent', 'token'],
  methods: [HttpMethod.POST],
  injectAuth: SlasProxyAuthType.SFDC_HEADER
}]);

/**
 * Allowed character set for the `{version}` and `{orgId}` path slots.
 * Matches the RFC 3986 unreserved set; legitimate SLAS org ids fit this shape.
 *
 * @private
 */
const SLAS_PATH_WILDCARD_SAFE = /^[A-Za-z0-9._~-]+$/;

/**
 * @private
 */
const RemoteServerFactory = exports.RemoteServerFactory = {
  /**
   * @private
   */
  _configure(options) {
    /**
     * Not all of these options are documented. Some exist to allow for
     * testing, or to handle non-standard projects.
     */
    const defaults = {
      // For test only – allow the project dir to be overridden.
      projectDir: process.cwd(),
      // Absolute path to the build directory
      buildDir: _path.default.resolve(process.cwd(), _constants.BUILD),
      // The cache time for SSR'd pages (defaults to 600 seconds)
      defaultCacheTimeSeconds: 600,
      // The port that the local dev server listens on
      port: 3443,
      // The protocol that the local dev server listens on
      protocol: 'https',
      // Whether or not to use a keep alive agent for proxy connections.
      proxyKeepAliveAgent: true,
      // Quiet flag (suppresses output if true)
      quiet: false,
      // Suppress SSL checks - can be used for local dev server
      // test code. Undocumented at present because there should
      // be no use-case for SDK users to set this.
      strictSSL: true,
      mobify: undefined,
      // Toggle cookies being passed and set
      localAllowCookies: false,
      // Toggle for setting up the custom SLAS private client secret handler
      useSLASPrivateClient: false,
      // Allow-list of SLAS endpoints the private-client proxy will
      // forward. When undefined, the built-in
      // `SLAS_PRIVATE_PROXY_ALLOWLIST` is used. Supplying a custom list
      // is security-sensitive and logs a startup warning.
      slasPrivateClientAllowList: undefined,
      // A regex for identifying which SLAS endpoints return tokens (access_token, refresh_token)
      // in the response body. Used to determine which responses should have HttpOnly session
      // cookies applied when that feature is enabled. Users can override this in ssr.js.
      tokenResponseEndpoints: _constants.SLAS_TOKEN_RESPONSE_ENDPOINTS,
      // Custom callback to modify the SLAS private client proxy request. This callback is invoked
      // after the built-in proxy request handling. Users can provide additional
      // request modifications (e.g., custom headers).
      // Signature: (proxyRequest, incomingRequest, res) => void
      onSLASPrivateProxyReq: undefined,
      // Custom callback to modify the SLAS private client proxy response. This callback is invoked
      // after the built-in proxy response handling (including HttpOnly session cookie handling when enabled).
      // When HttpOnly session cookies are enabled (MRT_ENABLE_HTTPONLY_SESSION_COOKIES=true), the callback
      // receives the response with tokens already moved to HttpOnly cookies and stripped from the body.
      // Custom callbacks must not rely on token fields in the response body in that case; read from
      // response headers (e.g. Set-Cookie) if needed.
      // Signature: (responseBuffer, proxyRes, req, res) => Buffer
      onSLASPrivateProxyRes: undefined,
      // Custom callback to modify the SLAS public client proxy request. This callback is invoked
      // after the built-in proxy request handling. Users can provide additional
      // request modifications (e.g., custom headers).
      // Signature: (proxyRequest, incomingRequest, res) => void
      onSLASPublicProxyReq: undefined,
      // Custom callback to modify the SLAS public client proxy response. This callback is invoked
      // after the built-in proxy response handling (including HttpOnly session cookie handling).
      // Signature: (responseBuffer, proxyRes, req, res) => Buffer
      onSLASPublicProxyRes: undefined
    };
    if (options && 'applySLASPrivateClientToEndpoints' in options) {
      _loggerInstance.default.warn('`applySLASPrivateClientToEndpoints` is deprecated. The SLAS private-client proxy is now gated by a built-in allow-list; when the legacy option is supplied, it is applied as a narrowing-only filter for backwards compatibility (it can remove entries from the allow-list, never add). Remove this option from your `ssr.js` and, if you need to widen the set of proxied endpoints, use `slasPrivateClientAllowList` instead.', {
        namespace: 'RemoteServerFactory._configure'
      });
    }
    options = _extends({}, defaults, options);
    (0, _ssrServer.setQuiet)(options.quiet || process.env.SSR_QUIET);

    // Set the protocol for the Express app listener - defaults to https on remote
    options.protocol = this._getProtocol(options);

    // Local dev server doesn't cache by default
    options.defaultCacheControl = this._getDefaultCacheControl(options);

    // Ensure this is a boolean, and is always true for a remote server.
    options.strictSSL = this._strictSSL(options);

    // This is the external HOSTNAME under which we are serving the page.
    // The EXTERNAL_DOMAIN_NAME value technically only applies to remote
    // operation, but we allow it to be used for a local dev server also.
    options.appHostname = process.env.EXTERNAL_DOMAIN_NAME || `localhost:${options.port}`;
    options.devServerHostName = process.env.LISTEN_ADDRESS || `localhost:${options.port}`;

    // This is the ORIGIN under which we are serving the page.
    // because it's an origin, it does not end with a slash.
    options.appOrigin = process.env.APP_ORIGIN = `${options.protocol}://${options.appHostname}`;

    // Toggle cookies being passed and set. Can be overridden locally,
    // always uses MRT_ALLOW_COOKIES env remotely
    options.allowCookies = this._getAllowCookies(options);

    // For test only – configure the SLAS private client secret proxy endpoint
    options.slasHostName = this._getSlasEndpoint(options);
    options.slasTarget = options.slasTarget || `https://${options.slasHostName}`;
    return options;
  },
  /**
   * @private
   */
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _logStartupMessage(options) {
    // Hook for the DevServer
  },
  /**
   * @private
   */
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _getAllowCookies(options) {
    return 'MRT_ALLOW_COOKIES' in process.env ? process.env.MRT_ALLOW_COOKIES == 'true' : false;
  },
  /**
   * @private
   */
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _getProtocol(options) {
    return 'https';
  },
  /**
   * @private
   */
  _getDefaultCacheControl(options) {
    return `max-age=${options.defaultCacheTimeSeconds}, s-maxage=${options.defaultCacheTimeSeconds}`;
  },
  /**
   * @private
   */
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _strictSSL(options) {
    return true;
  },
  /**
   * @private
   */
  _isBundleOrProxyPath(url) {
    return url.startsWith(_ssrNamespacePaths.proxyBasePath) || url.startsWith(_ssrNamespacePaths.bundleBasePath);
  },
  /**
   * @private
   */
  _getSlasEndpoint(options) {
    var _options$mobify, _options$mobify$app, _options$mobify$app$c, _options$mobify$app$c2;
    if (!options.useSLASPrivateClient && process.env.MRT_ENABLE_HTTPONLY_SESSION_COOKIES !== 'true') {
      return undefined;
    }
    const shortCode = (_options$mobify = options.mobify) === null || _options$mobify === void 0 ? void 0 : (_options$mobify$app = _options$mobify.app) === null || _options$mobify$app === void 0 ? void 0 : (_options$mobify$app$c = _options$mobify$app.commerceAPI) === null || _options$mobify$app$c === void 0 ? void 0 : (_options$mobify$app$c2 = _options$mobify$app$c.parameters) === null || _options$mobify$app$c2 === void 0 ? void 0 : _options$mobify$app$c2.shortCode;
    return `${shortCode}.api.commercecloud.salesforce.com`;
  },
  /**
   * @private
   */
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _setCompression(app) {
    // Let the CDN do it
  },
  /**
   * @private
   */

  _setupLogging(app) {
    const morganLoggerFormat = function (tokens, req, res) {
      const contentLength = tokens.res(req, res, 'content-length');
      return [`(${res.locals.requestId})`, tokens.method(req, res), tokens.url(req, res), tokens.status(req, res), tokens['response-time'](req, res), 'ms', contentLength && `- ${contentLength}`].join(' ');
    };

    // Morgan stream for logging status codes less than 400
    app.use((0, _morgan.default)(morganLoggerFormat, {
      skip: function (req, res) {
        return res.statusCode >= 400;
      },
      stream: {
        write: message => {
          _loggerInstance.default.info(message, {
            namespace: 'httprequest'
          });
        }
      }
    }));

    // Morgan stream for logging status codes 400 and above
    app.use((0, _morgan.default)(morganLoggerFormat, {
      skip: function (req, res) {
        return res.statusCode < 400;
      },
      stream: {
        write: message => {
          _loggerInstance.default.error(message, {
            namespace: 'httprequest'
          });
        }
      }
    }));
  },
  /**
   * Passing the correlation Id from MRT to locals
   * @private
   */
  _setRequestId(app) {
    app.use((req, res, next) => {
      const correlationId = req.headers['x-correlation-id'];
      const requestId = correlationId ? correlationId : req.headers['x-apigateway-event'];
      if (!requestId) {
        _loggerInstance.default.error('Both x-correlation-id and x-apigateway-event headers are missing', {
          namespace: '_setRequestId'
        });
        next();
        return;
      }
      res.locals.requestId = requestId;
      next();
    });
  },
  /**
   * @private
   */
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _setupMetricsFlushing(app) {
    // Hook for the dev-server
  },
  /**
   * @private
   */
  _updatePackageMobify(options) {
    (0, _ssrShared.updatePackageMobify)(options.mobify);
  },
  /**
   * @private
   */
  _configureProxyConfigs(options) {
    (0, _ssrServer.configureProxyConfigs)(options.appHostname, options.protocol);
  },
  /**
   * @private
   */
  _createApp(options) {
    options = this._configure(options);
    this._logStartupMessage(options);

    // To gain a small speed increase in the event that this
    // server needs to make a proxy request back to itself,
    // we kick off a DNS lookup for the appHostname. We don't
    // wait for it to complete, or care if it fails, so the
    // callback is a no-op.
    _dns.default.lookup(options.appHostname, () => null);
    this._validateConfiguration(options);
    this._updatePackageMobify(options);
    this._configureProxyConfigs(options);
    const app = this._createExpressApp(options);

    // Do this first – we want compression applied to
    // everything when it's enabled at all.
    this._setCompression(app);
    this._setRequestId(app);
    // this._addEventContext(app)

    // We want to remove any base paths that have made it this far.
    // Base paths are used to route requests to the correct server.
    // If the request has reached the server, it is no longer needed.
    // Note: We use envBasePath as the feature flag for this middleware
    // If envBasePath is `/`, '', or undefined when the server starts, we don't need to
    // initialize this middleware.
    if ((0, _ssrNamespacePaths.getEnvBasePath)()) {
      this._setupBasePathMiddleware(app);
    }

    // Ordering of the next two calls are vital - we don't
    // want request-processors applied to development views.
    this._addSDKInternalHandlers(app);
    this._setupSSRRequestProcessorMiddleware(app);
    this._setForwardedHeaders(app, options);
    this._setupLogging(app);
    this._setupMetricsFlushing(app);
    this._setupHealthcheck(app);
    this._setupProxying(app, options);
    const httpOnlyCookiesEnabled = process.env.MRT_ENABLE_HTTPONLY_SESSION_COOKIES === 'true';
    if (options.useSLASPrivateClient) {
      this._setupSlasPrivateClientProxy(app, options, httpOnlyCookiesEnabled);
    } else if (httpOnlyCookiesEnabled) {
      this._setupSlasPublicClientProxy(app, options, httpOnlyCookiesEnabled);
    }
    this._setupHybridProxy(app, options);

    // Beyond this point, we know that this is not a proxy request
    // and not a bundle request, so we can apply specific
    // processing.
    this._setupCommonMiddleware(app, options);
    this._addStaticAssetServing(app);
    this._addDevServerGarbageCollection(app);
    return app;
  },
  _setupHybridProxy(app, options) {
    var _options$hybridProxy;
    if ((_options$hybridProxy = options.hybridProxy) !== null && _options$hybridProxy !== void 0 && _options$hybridProxy.enabled) {
      app.use((0, _hybridProxy.hybridProxy)(options));
    }
  },
  /**
   * @private
   */
  _createExpressApp(options) {
    const app = (0, _express.default)();
    app.disable('x-powered-by');
    const mixin = {
      options,
      // Forcing a GC is no longer necessary, and will be
      // skipped by default (unless FORCE_GC env-var is set).
      _collectGarbage() {
        // Do global.gc in a separate 'then' handler so
        // that all major variables are out of scope and
        // eligible for garbage collection.

        /* istanbul ignore next */
        if (global.gc) {
          global.gc();
        }
      },
      _requestMonitor: new RequestMonitor(),
      metrics: _ssrServer.MetricsSender.getSender(),
      /**
       * Emitting per-request CloudWatch custom metrics has been removed to
       * eliminate the PutMetricData cost incurred on every request (W-22715301).
       *
       * This method is intentionally retained as a no-op for backwards
       * compatibility: it is exposed on the Express `app` and may be called
       * by customer code. It accepts the same arguments as before and does
       * nothing. The underlying MetricsSender (`this.metrics`) is left in
       * place for the same reason.
       *
       * @private
       * @param name {String} metric name (ignored)
       * @param [value] {Number} metric value (ignored)
       * @param [unit] {String} metric unit (ignored)
       * @param [dimensions] {Object} optional extra dimensions (ignored)
       */
      // eslint-disable-next-line @typescript-eslint/no-unused-vars
      sendMetric(name, value = 1, unit = 'Count', dimensions) {
        // no-op: custom metric emission removed (W-22715301)
      },
      get applicationCache() {
        if (!this._applicationCache) {
          this._applicationCache = new _ssrCache.PersistentCache();
        }
        return this._applicationCache;
      }
    };
    (0, _mergeDescriptors.default)(app, mixin);
    return app;
  },
  /**
   * @private
   */
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _addSDKInternalHandlers(app) {
    // This method is used by the dev server, but is not needed here.
  },
  /**
   * Set x-forward-* headers into locals, this is primarily used to facilitate react sdk hook `useOrigin`
   * @private
   */
  _setForwardedHeaders(app, options) {
    app.use((req, res, next) => {
      var _req$headers, _req$headers2;
      const xForwardedHost = (_req$headers = req.headers) === null || _req$headers === void 0 ? void 0 : _req$headers['x-forwarded-host'];
      const xForwardedProto = (_req$headers2 = req.headers) === null || _req$headers2 === void 0 ? void 0 : _req$headers2['x-forwarded-proto'];
      if (xForwardedHost) {
        // prettier-ignore
        res.locals.xForwardedOrigin = `${xForwardedProto || options.protocol}://${xForwardedHost}`;
      }
      next();
    });
  },
  /**
   * @private
   */
  _setupBasePathMiddleware(app) {
    var _getConfig, _getConfig$app, _getConfig$app$url;
    // Cache the express route regexes to avoid re-calculating them on every request.
    let expressRouteRegexes;

    /**
     * Remove the base path from a path.
     *
     * If path is like '/basepath/something', this returns '/something'
     * If path is exactly '/basepath', this returns '/'
     * If path doesn't start with base path or if there is no base path defined,
     * returns the unmodified path
     *
     * @param path {string} the path to remove the base path from
     * @returns {string} the path with the base path removed
     */
    const removeBasePathFromPath = path => {
      const basePath = (0, _ssrNamespacePaths.getEnvBasePath)();
      if (!basePath) {
        return path;
      }
      if (path.startsWith(basePath + '/')) {
        return path.substring(basePath.length);
      } else if (path === basePath) {
        return '/';
      }
      return path;
    };

    /**
     * Initializes a cache of regexes that correspond to the registered express routes
     *
     * This specifically omits the generic wildcard from the express routes where we
     * want to remove the base path from since it is mapped to the app render. This is
     * because routes sent to the render are handled by React Router
     */
    const initializeExpressRouteRegexes = () => {
      const expressRoutes = app._router.stack.filter(layer => layer.route && layer.route.path && layer.route.path !== '*').map(layer => layer.route.path);
      expressRouteRegexes = expressRoutes.map(route => (0, _convertExpressRoute.convertExpressRouteToRegex)(route));
    };

    /**
     * Very early request processing.
     *
     * If the server receives a request containing the base path, remove it before allowing
     * the request through to the other express endpoints
     *
     * We scope base path removal to /mobify routes, /__pwa-kit routes (when showBasePath
     * is false), and routes defined by the express app (For example /callback or /worker.js).
     * This is to avoid affecting React Router routes where a site id or locale might be present
     * that is equal to the base path.
     *
     * /__pwa-kit routes are a special case. These are internal PWA Kit routes
     * (e.g. /__pwa-kit/refresh) that are registered as React Router routes and are invoked
     * by the Storefront Preview code on Runtime Admin (which always appends a base path if set).
     * When showBasePath is false, React Router has no basename, so we redirect to the clean
     * URL (without base path) so the browser navigates to a path that React Router can match
     * and hydrate correctly on the client.
     * When showBasePath is true, React Router handles the base path via its basename prop.
     *
     * For example, if you have a base path of /us and a site id of /us we don't want
     * to remove the /us from www.example.com/us/en-US/category/... as this route is handled by
     * React Router and the PWA multisite implementation.
     *
     * @param req {express.req} the incoming request - modified in-place
     * @private
     */
    const showBasePath = ((_getConfig = (0, _ssrConfig.getConfig)()) === null || _getConfig === void 0 ? void 0 : (_getConfig$app = _getConfig.app) === null || _getConfig$app === void 0 ? void 0 : (_getConfig$app$url = _getConfig$app.url) === null || _getConfig$app$url === void 0 ? void 0 : _getConfig$app$url.showBasePath) === true;
    const removeBasePathMiddleware = (req, res, next) => {
      const basePath = (0, _ssrNamespacePaths.getEnvBasePath)();

      // Fast path: /mobify routes always get the base path removed
      if (req.path.startsWith(`${basePath}/mobify`)) {
        const cleanPath = removeBasePathFromPath(req.path);
        const {
          search
        } = (0, _ssrServer.parseRequestUrl)(req);
        req.url = cleanPath + search;
        return next();
      }

      // /__pwa-kit routes: when showBasePath is false, the browser URL still has the
      // base path but client-side React Router has no basename, so hydration would fail.
      // Redirect to the clean URL so the browser navigates to a path React Router matches.
      if (!showBasePath && req.path.startsWith(`${basePath}/__pwa-kit`)) {
        const cleanPath = removeBasePathFromPath(req.path);
        const {
          search
        } = (0, _ssrServer.parseRequestUrl)(req);
        return res.redirect(302, cleanPath + search);
      }

      // For other routes, only proceed if path equals basePath or path starts with basePath + '/'
      const pathMatchesBasePath = req.path === basePath || req.path.startsWith(basePath + '/');
      if (!pathMatchesBasePath) {
        return next();
      }

      // Now we know path starts with base path, so we can remove it
      const cleanPath = removeBasePathFromPath(req.path);

      // Initialize express route regexes if needed
      // We do this here since we want to ensure that any express route defined
      // after the app is created, such as routes defined in ssr.js, are included.
      if (!expressRouteRegexes) {
        initializeExpressRouteRegexes();
      }

      // Next we check if the clean path matches any existing express routes
      // (like /callback or /worker.js)
      const matchesExpressRoute = expressRouteRegexes.some(routeRegex => {
        try {
          return routeRegex.test(cleanPath);
        } catch (error) {
          _loggerInstance.default.warn(`Invalid express route pattern: ${routeRegex}`, /* istanbul ignore next */
          {
            namespace: 'removeBasePathMiddleware',
            additionalProperties: {
              error: error
            }
          });
          return false;
        }
      });

      // Only update URL if our clean path matches an Express route
      // This leaves React Router paths (like /en-US/category) unchanged
      if (matchesExpressRoute) {
        const {
          search
        } = (0, _ssrServer.parseRequestUrl)(req);
        req.url = cleanPath + search;
      }
      next();
    };
    app.use(removeBasePathMiddleware);
  },
  /**
   * @private
   */
  _setupSSRRequestProcessorMiddleware(app) {
    // Attach this middleware as early as possible. It does timing
    // and applies some early processing that must occur before
    // anything else.

    /**
     * Incoming request processing.
     *
     * For the local dev server, if there is a request processor, use it to
     * process all non-proxy, non-bundle requests, in the same way that
     * CloudFront will do for a deployed bundle.
     *
     * If there is an x-querystring header in the incoming request, use
     * that as the definitive querystring.
     *
     * @param req {express.req} the incoming request - modified in-place
     * @param res {express.res} the response object
     * @private
     */
    const processIncomingRequest = (req, res) => {
      const options = req.app.options;
      // If the request is for a proxy or bundle path, do nothing
      if (this._isBundleOrProxyPath(req.originalUrl)) {
        return;
      }

      // Apply the request processor
      // `this` is bound to the calling context, usually RemoteServerFactory
      const requestProcessor = this._getRequestProcessor(req);
      let {
        search,
        query: originalQuerystring
      } = (0, _ssrServer.parseRequestUrl)(req);
      let updatedQuerystring = originalQuerystring;
      let updatedPath = req.path;

      // If there's an x-querystring header, use that as the definitive
      // querystring. This header is used in production, not in local dev,
      // but we always handle it here to allow for testing.
      const xQueryString = req.headers[_constants.X_MOBIFY_QUERYSTRING];
      if (xQueryString) {
        updatedQuerystring = xQueryString;
        // Hide the header from any other code
        delete req.headers[_constants.X_MOBIFY_QUERYSTRING];
      }
      if (requestProcessor) {
        // Allow the processor to handle this request. Because this code
        // runs only in the local development server, we intentionally do
        // not swallow errors - we want them to happen and show up on the
        // console because that's how developers can test the processor.
        const headers = new _ssrProxying.Headers(req.headers, 'http');
        const processed = requestProcessor.processRequest({
          headers,
          path: req.path,
          querystring: updatedQuerystring,
          getRequestClass: () => headers.getHeader(_ssrProxying.X_MOBIFY_REQUEST_CLASS),
          setRequestClass: value => headers.setHeader(_ssrProxying.X_MOBIFY_REQUEST_CLASS, value),
          // This matches the set of parameters passed in the
          // Lambda@Edge context.
          parameters: {
            deployTarget: `${process.env.DEPLOY_TARGET || 'local'}`,
            appHostname: options.appHostname,
            proxyConfigs: _ssrShared.proxyConfigs
          }
        });

        // Aid debugging by checking the return value
        (0, _assert.default)(processed && 'path' in processed && 'querystring' in processed, 'Expected processRequest to return an object with ' + '"path" and "querystring" properties, ' + `but got ${JSON.stringify(processed, null, 2)}`);

        // Update the request.
        updatedQuerystring = processed.querystring;
        updatedPath = processed.path;
        if (headers.modified) {
          req.headers = headers.toObject();
        }
      }

      // Update the request.
      if (updatedQuerystring !== originalQuerystring) {
        // Update the search string to reflect the new querystring
        search = updatedQuerystring ? `?${updatedQuerystring}` : '';

        // Let Express re-parse the parameters
        if (updatedQuerystring) {
          const queryStringParser = req.app.set('query parser fn');
          req.query = queryStringParser(updatedQuerystring);
        } else {
          req.query = {};
        }
      }

      // This will update the request's URL with the new path
      // and querystring.
      req.url = updatedPath + search;

      // Get the request class and store it for general use. We
      // must do this AFTER the request-processor, because that's
      // what may set the request class.
      res.locals.requestClass = req.headers[_ssrProxying.X_MOBIFY_REQUEST_CLASS];
    };
    const ssrRequestProcessorMiddleware = (req, res, next) => {
      const locals = res.locals;
      locals.afterResponseCalled = false;
      locals.responseCaching = {};
      locals.originalUrl = req.originalUrl;

      // Track this response
      req.app._requestMonitor._responseStarted(res);

      // If the path is /, we enforce that the only methods
      // allowed are GET, HEAD or OPTIONS. This is a restriction
      // imposed by API Gateway: we enforce it here so that the
      // local dev server has the same behaviour.
      if (req.path === '/' && !['GET', 'HEAD', 'OPTIONS'].includes(req.method)) {
        res.sendStatus(405);
        return;
      }

      // Apply custom query parameter parsing.
      processIncomingRequest(req, res);
      const afterResponse = () => {
        /* istanbul ignore else */
        if (!locals.afterResponseCalled) {
          locals.afterResponseCalled = true;

          // Clean up the per-request QueryClient to prevent server-side
          // cache retention across warm Lambda invocations. Without this,
          // cached query data and related timers can persist beyond the
          // request lifecycle when cacheTime > 0, increasing memory
          // pressure and triggering GC spikes.
          const queryClient = locals.__queryClient;
          if (queryClient) {
            queryClient.clear();
            locals.__queryClient = null;
          }

          // Per-request custom CloudWatch metrics (RequestTime,
          // RequestSuccess, RequestFailed4xx/5xx) were removed to
          // eliminate the PutMetricData cost on every request
          // (W-22715301).
        }
      };

      // Attach event listeners to the Response (we need to attach
      // both to handle all possible cases)
      res.on('finish', afterResponse);
      res.on('close', afterResponse);

      // Strip out API Gateway headers from the incoming request. We
      // do that now so that the rest of the code don't have to deal
      // with these headers, which can be large and may be accidentally
      // forwarded to other servers.
      _ssrProxying.X_HEADERS_TO_REMOVE_ORIGIN.forEach(key => {
        delete req.headers[key];
      });

      // Hand off to the next middleware
      next();
    };
    app.use(ssrRequestProcessorMiddleware);
  },
  /**
   * @private
   */
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _setupProxying(app, options) {
    app.all(`${_ssrNamespacePaths.proxyBasePath}/*`, (_, res) => {
      return res.status(501).json({
        message: 'Environment proxies are not set: https://developer.salesforce.com/docs/commerce/pwa-kit-managed-runtime/guide/proxying-requests.html'
      });
    });
  },
  /**
   * @private
   */
  _handleMissingSlasPrivateEnvVar(app, slasPrivateProxyPath) {
    app.use(slasPrivateProxyPath, (_, res) => {
      return res.status(501).json({
        message: 'Environment variable PWA_KIT_SLAS_CLIENT_SECRET not set: Please set this environment variable to proceed.'
      });
    });
  },
  /**
   * URL-decode and normalize a request path for consistent security checks.
   *
   * Express exposes `req.path` in its raw, percent-encoded form while
   * upstream services decode and normalize before routing. Matching
   * against the raw value alone would miss equivalent encoded forms,
   * so we decode until stable and collapse dot-segments to ensure
   * consistent path evaluation regardless of encoding depth.
   *
   * @param {string} rawPath - The raw, potentially percent-encoded path
   * @returns {(string | null)} The decoded/normalized path or `null` if decoding fails
   * @private
   */
  _normalizeSlasPath(rawPath) {
    try {
      // Decode iteratively until the value stabilizes, so that double-encoded,
      // triple-encoded, etc. sequences are fully resolved before security checks run.
      let decoded = rawPath;
      let prev;
      do {
        prev = decoded;
        decoded = decodeURIComponent(decoded);
      } while (decoded !== prev);
      return _path.posix.normalize(decoded);
    } catch {
      // If decoding fails (e.g. malformed percent encoding), we cannot determine what
      // path the upstream would resolve. Return null to signal that the caller should
      // reject the request.
      return null;
    }
  },
  /**
   * Match a normalized path + method against the configured allow-list.
   *
   * Returns the matched entry or `null`. The path must start with
   * `/shopper/auth/{version}/organizations/{orgId}/` and its trailing
   * segments must equal one of the entries' `segments`. The `{version}` and
   * `{orgId}` slots are constrained to `SLAS_PATH_WILDCARD_SAFE`.
   *
   * @param {string|null} normalizedPath - Output of `_normalizeSlasPath`
   * @param {string} method - HTTP method on the incoming request
   * @param {Array<{segments: string[], methods: string[], injectAuth: string}>} [allowList]
   * @returns {{segments: string[], methods: string[], injectAuth: string}|null}
   * @private
   */
  _matchSlasAllowlistEntry(normalizedPath, method, allowList) {
    if (!normalizedPath) {
      return null;
    }
    const list = Array.isArray(allowList) ? allowList : SLAS_PRIVATE_PROXY_ALLOWLIST;
    const segments = normalizedPath.split('/').filter(Boolean);
    if (segments.length < 6 || segments[0] !== 'shopper' || segments[1] !== 'auth' || segments[3] !== 'organizations') {
      return null;
    }
    if (!SLAS_PATH_WILDCARD_SAFE.test(segments[2]) || !SLAS_PATH_WILDCARD_SAFE.test(segments[4])) {
      return null;
    }
    const upperMethod = String(method || '').toUpperCase();
    const tail = segments.slice(5);
    return list.find(entry => Array.isArray(entry === null || entry === void 0 ? void 0 : entry.segments) && Array.isArray(entry === null || entry === void 0 ? void 0 : entry.methods) && entry.methods.includes(upperMethod) && entry.segments.length === tail.length && entry.segments.every((seg, i) => seg === tail[i])) ?? null;
  },
  /**
   * Strip the proxy base path and normalize the SLAS path for forwarding.
   * Normalizes only the path portion so that URLs in query parameters
   * (e.g. redirect_uri=http://localhost:3000/callback) are not mangled
   * by posixPath.normalize collapsing `://` to `:/`.
   * @private
   */
  _rewriteSlasProxyPath(rawPath, proxyPath) {
    // Example input:
    //   rawPath   = "/mobify/slas/private/shopper/auth/.../authorize?redirect_uri=http://localhost:3000/callback"
    //   proxyPath = "/mobify/slas/private"

    // Strip the proxy prefix (and optional base path).
    //   → "/shopper/auth/.../authorize?redirect_uri=http://localhost:3000/callback"
    const basePathRegexEntry = (0, _ssrNamespacePaths.getEnvBasePath)() ? `${(0, _ssrNamespacePaths.getEnvBasePath)()}?` : '';
    const regex = new RegExp(`^${basePathRegexEntry}${proxyPath}`);
    const stripped = rawPath.replace(regex, '');

    // Split path from query string BEFORE normalizing.
    // posixPath.normalize would collapse "://" in redirect_uri=http://... to ":/".
    //   pathname   = "/shopper/auth/.../authorize"
    //   queryParts = ["redirect_uri=http://localhost:3000/callback"]
    const [pathname, ...queryParts] = stripped.split('?');

    // Normalize only the path (decode percent-encoding, resolve dot-segments).
    //   → "/shopper/auth/v1/organizations/org1/oauth2/authorize"
    const normalizedPath = this._normalizeSlasPath(pathname);
    if (normalizedPath === null) return null;

    // Re-attach the query string unchanged.
    //   → "/shopper/auth/.../authorize?redirect_uri=http://localhost:3000/callback"
    return queryParts.length ? `${normalizedPath}?${queryParts.join('?')}` : normalizedPath;
  },
  /**
   * Express middleware that gates requests against the SLAS allowlist.
   * Normalizes the path, matches it against the allowlist, and returns 403
   * for unmatched requests. Stashes the match on `req` for downstream use.
   * @private
   */
  _createSlasAllowlistGuard(allowList, proxyLabel) {
    return (req, res, next) => {
      const normalizedPath = this._normalizeSlasPath(req.path);
      const allowedEntry = this._matchSlasAllowlistEntry(normalizedPath, req.method, allowList);
      if (!allowedEntry) {
        const message = `Request to ${req.path} is not allowed through the SLAS ${proxyLabel} Client Proxy`;
        _loggerInstance.default.error(message);
        return res.status(403).json({
          message
        });
      }
      req._normalizedSlasPath = normalizedPath;
      req._slasAllowlistEntry = allowedEntry;
      next();
    };
  },
  /**
   * @private
   */
  _setupSlasPrivateClientProxy(app, options, httpOnlyCookiesEnabled) {
    var _options$mobify2, _options$mobify2$app, _options$mobify2$app$, _options$mobify2$app$2;
    if (!options.useSLASPrivateClient) {
      return;
    }
    (0, _ssrServer.localDevLog)(`Proxying ${_ssrNamespacePaths.slasPrivateProxyPath} to ${options.slasTarget}`);
    const clientId = (_options$mobify2 = options.mobify) === null || _options$mobify2 === void 0 ? void 0 : (_options$mobify2$app = _options$mobify2.app) === null || _options$mobify2$app === void 0 ? void 0 : (_options$mobify2$app$ = _options$mobify2$app.commerceAPI) === null || _options$mobify2$app$ === void 0 ? void 0 : (_options$mobify2$app$2 = _options$mobify2$app$.parameters) === null || _options$mobify2$app$2 === void 0 ? void 0 : _options$mobify2$app$2.clientId;
    const clientSecret = process.env.PWA_KIT_SLAS_CLIENT_SECRET;
    if (!clientSecret) {
      this._handleMissingSlasPrivateEnvVar(app, _ssrNamespacePaths.slasPrivateProxyPath);
      return;
    }
    const encodedSlasCredentials = Buffer.from(`${clientId}:${clientSecret}`).toString('base64');
    const hasCustomAllowList = Array.isArray(options.slasPrivateClientAllowList);
    if (hasCustomAllowList) {
      _loggerInstance.default.warn('A custom `slasPrivateClientAllowList` is in use. Overriding the built-in allow-list widens the surface of SLAS endpoints reachable through the private-client proxy and can re-introduce credential exposure to endpoints the platform does not intend the storefront to reach. Audit each entry carefully.', {
        namespace: '_setupSlasPrivateClientProxy'
      });
    }
    let allowList = hasCustomAllowList ? options.slasPrivateClientAllowList : SLAS_PRIVATE_PROXY_ALLOWLIST;

    // Backwards-compat: the deprecated `applySLASPrivateClientToEndpoints`
    // regex is applied as a narrowing-only filter on the allow-list. It can
    // remove entries but never add. Customers who need to widen the allow
    // list should migrate to `slasPrivateClientAllowList`. The deprecation
    // warning is emitted by `_configure`.
    const legacyRegex = options.applySLASPrivateClientToEndpoints;
    if (legacyRegex instanceof RegExp) {
      allowList = allowList.filter(entry => legacyRegex.test(`/${entry.segments.join('/')}`));
      if (allowList.length === 0) {
        _loggerInstance.default.warn('`applySLASPrivateClientToEndpoints` removed every entry from the SLAS private-client allow-list. The proxy will reject all requests. Remove the legacy option or update it to match at least one allow-list entry.', {
          namespace: '_setupSlasPrivateClientProxy'
        });
      }
    }
    app.use(_ssrNamespacePaths.slasPrivateProxyPath, this._createSlasAllowlistGuard(allowList, 'Private'), (0, _httpProxyMiddleware.createProxyMiddleware)({
      target: options.slasTarget,
      changeOrigin: true,
      // http-proxy-middleware uses the original incoming request path to determine
      // both proxyRequest and incomingRequest paths.
      // This cannot be modified by any express middleware
      // So we need to use the built in pathRewrite to remove the base path
      pathRewrite: path => this._rewriteSlasProxyPath(path, _ssrNamespacePaths.slasPrivateProxyPath),
      selfHandleResponse: true,
      onProxyReq: (proxyRequest, incomingRequest, res) => {
        try {
          (0, _configureProxy.applyProxyRequestHeaders)({
            proxyRequest,
            incomingRequest,
            proxyPath: _ssrNamespacePaths.slasPrivateProxyPath,
            targetHost: options.slasHostName,
            targetProtocol: 'https'
          });

          // Internal preview-context signal consumed by the response
          // interceptor (process-token-response); never forwarded to
          // SLAS, regardless of the HttpOnly session-cookie flag.
          proxyRequest.removeHeader(_constants.X_PREVIEW_PARENT);

          // Credential mode is taken from the allow-list entry
          // attached by the pre-proxy guard.
          const {
            injectAuth = SlasProxyAuthType.NONE
          } = incomingRequest._slasAllowlistEntry ?? {};
          if (injectAuth === SlasProxyAuthType.SFDC_HEADER) {
            proxyRequest.setHeader('_sfdc_client_auth', encodedSlasCredentials);
          } else if (injectAuth === SlasProxyAuthType.BASIC) {
            proxyRequest.setHeader('Authorization', `Basic ${encodedSlasCredentials}`);
          }
          if (httpOnlyCookiesEnabled) {
            handleHttpOnlyCookiesOnProxyReq(proxyRequest, incomingRequest);
          }

          // Allow users to apply additional custom modifications to the proxy request
          if (typeof options.onSLASPrivateProxyReq === 'function') {
            try {
              options.onSLASPrivateProxyReq(proxyRequest, incomingRequest, res);
            } catch (error) {
              _loggerInstance.default.error('Error in custom onSLASPrivateProxyReq callback', {
                namespace: '_setupSlasPrivateClientProxy',
                additionalProperties: {
                  error
                }
              });
            }
          }
        } catch (error) {
          if (error instanceof RefreshTokenNotFoundError) {
            _loggerInstance.default.warn(error.message, {
              namespace: 'setRefreshTokenHeader'
            });
            if (!res.headersSent) {
              proxyRequest.destroy();
              res.status(401).json({
                message: 'invalid refresh_token'
              });
            }
            return;
          }
          _loggerInstance.default.error('Error in SLAS private proxy request handling', {
            namespace: '_setupSlasPrivateClientProxy',
            additionalProperties: {
              error
            }
          });
          if (!res.headersSent) {
            res.status(500).json({
              message: 'Error preparing SLAS private proxy request'
            });
          }
        }
      },
      onError: (error, req, res) => {
        _loggerInstance.default.error('Error in SLAS private proxy', {
          namespace: '_setupSlasPrivateClientProxy',
          additionalProperties: {
            error,
            path: req === null || req === void 0 ? void 0 : req.url
          }
        });
        if (!res.headersSent) {
          res.status(500).json({
            message: 'Error in SLAS private proxy request'
          });
        }
      },
      onProxyRes: (0, _httpProxyMiddleware.responseInterceptor)((responseBuffer, proxyRes, req, res) => {
        let workingBuffer = responseBuffer;
        try {
          var _req$_slasAllowlistEn;
          if (httpOnlyCookiesEnabled) {
            try {
              workingBuffer = handleHttpOnlyCookiesOnProxyRes(workingBuffer, proxyRes, req, res, options, '_setupSlasPrivateClientProxy');
            } catch (error) {
              res.statusCode = 500;
              res.statusMessage = 'Internal Server Error';
              _loggerInstance.default.error('Error applying HttpOnly session cookies', {
                namespace: '_setupSlasPrivateClientProxy',
                additionalProperties: {
                  error: error.message || error
                }
              });
              return Buffer.from(JSON.stringify({
                error: 'Internal server error',
                message: error.message || 'An error occurred processing the authentication response'
              }), 'utf8');
            }
          }

          // Mask a 404 on /oauth2/passwordless/login with a 200 to prevent
          // user enumeration. Match against the allow-list entry stashed by
          // the pre-proxy guard.
          const entrySegments = (_req$_slasAllowlistEn = req._slasAllowlistEntry) === null || _req$_slasAllowlistEn === void 0 ? void 0 : _req$_slasAllowlistEn.segments;
          const isPasswordlessLogin = (entrySegments === null || entrySegments === void 0 ? void 0 : entrySegments.length) === 3 && entrySegments[0] === 'oauth2' && entrySegments[1] === 'passwordless' && entrySegments[2] === 'login';
          if (isPasswordlessLogin && proxyRes.statusCode === 404) {
            res.statusCode = 200;
            res.statusMessage = 'OK';

            // When a /passwordless/login endpoint response returns 200, it has no body
            // so we return an empty body here to match an actual 200 response.
            workingBuffer = Buffer.from('', 'utf8');
          }

          // Allow users to apply additional custom modifications to the proxy response
          if (typeof options.onSLASPrivateProxyRes === 'function') {
            try {
              const customBuffer = options.onSLASPrivateProxyRes(responseBuffer, proxyRes, req, res);
              // Only use the custom buffer if it was returned
              if (customBuffer !== undefined) {
                workingBuffer = customBuffer;
              }
            } catch (error) {
              _loggerInstance.default.error('Error in custom onSLASPrivateProxyRes callback', /* istanbul ignore next */
              {
                namespace: '_setupSlasPrivateClientProxy',
                additionalProperties: {
                  error
                }
              });
            }
          }
          return workingBuffer;
        } catch (error) {
          _loggerInstance.default.error('There is an error processing the response from SLAS. Returning original response.', {
            namespace: '_setupSlasPrivateClientProxy',
            additionalProperties: {
              error,
              statusCode: proxyRes === null || proxyRes === void 0 ? void 0 : proxyRes.statusCode,
              path: req === null || req === void 0 ? void 0 : req.url
            }
          });
          return workingBuffer;
        }
      })
    }));
  },
  /**
   * @private
   */
  _setupSlasPublicClientProxy(app, options, httpOnlyCookiesEnabled) {
    const logNamespace = '_setupSlasPublicClientProxy';
    (0, _ssrServer.localDevLog)(`Proxying ${_ssrNamespacePaths.slasPublicProxyPath} to ${options.slasTarget}`);
    app.use(_ssrNamespacePaths.slasPublicProxyPath,
    // Lightweight guard: normalize the path and reject anything outside /shopper/auth/.
    // No per-endpoint allowlist — the public proxy does not inject credentials,
    // so there is no credential-leakage risk. SLAS rejects unauthorized calls itself.
    (req, res, next) => {
      const normalizedPath = this._normalizeSlasPath(req.path);
      if (!normalizedPath || !normalizedPath.startsWith('/shopper/auth/')) {
        const message = `Request to ${req.path} is not allowed through the SLAS Public Client Proxy`;
        _loggerInstance.default.error(message);
        return res.status(403).json({
          message
        });
      }
      req._normalizedSlasPath = normalizedPath;
      next();
    }, (0, _httpProxyMiddleware.createProxyMiddleware)({
      target: options.slasTarget,
      changeOrigin: true,
      pathRewrite: path => this._rewriteSlasProxyPath(path, _ssrNamespacePaths.slasPublicProxyPath),
      selfHandleResponse: true,
      onProxyReq: (proxyRequest, incomingRequest, res) => {
        try {
          (0, _configureProxy.applyProxyRequestHeaders)({
            proxyRequest,
            incomingRequest,
            proxyPath: _ssrNamespacePaths.slasPublicProxyPath,
            targetHost: options.slasHostName,
            targetProtocol: 'https'
          });

          // Internal preview-context signal consumed by the response
          // interceptor (process-token-response); never forwarded to
          // SLAS, regardless of the HttpOnly session-cookie flag.
          proxyRequest.removeHeader(_constants.X_PREVIEW_PARENT);
          if (httpOnlyCookiesEnabled) {
            handleHttpOnlyCookiesOnProxyReq(proxyRequest, incomingRequest);
          }
          if (typeof options.onSLASPublicProxyReq === 'function') {
            try {
              options.onSLASPublicProxyReq(proxyRequest, incomingRequest, res);
            } catch (error) {
              _loggerInstance.default.error('Error in custom onSLASPublicProxyReq callback', {
                namespace: logNamespace,
                additionalProperties: {
                  error
                }
              });
            }
          }
        } catch (error) {
          if (error instanceof RefreshTokenNotFoundError) {
            _loggerInstance.default.warn(error.message, {
              namespace: 'setRefreshTokenHeader'
            });
            if (!res.headersSent) {
              proxyRequest.destroy();
              res.status(401).json({
                message: 'invalid refresh_token'
              });
            }
            return;
          }
          _loggerInstance.default.error('Error in SLAS public proxy request handling', {
            namespace: logNamespace,
            additionalProperties: {
              error
            }
          });
          if (!res.headersSent) {
            res.status(500).json({
              message: 'Error preparing SLAS Public proxy request'
            });
          }
        }
      },
      onError: (error, req, res) => {
        _loggerInstance.default.error('Error in SLAS public proxy', {
          namespace: logNamespace,
          additionalProperties: {
            error,
            path: req === null || req === void 0 ? void 0 : req.url
          }
        });
        if (!res.headersSent) {
          res.status(500).json({
            message: 'Error in SLAS public proxy request'
          });
        }
      },
      onProxyRes: (0, _httpProxyMiddleware.responseInterceptor)((responseBuffer, proxyRes, req, res) => {
        let workingBuffer = responseBuffer;
        try {
          if (httpOnlyCookiesEnabled) {
            try {
              workingBuffer = handleHttpOnlyCookiesOnProxyRes(workingBuffer, proxyRes, req, res, options, logNamespace);
            } catch (error) {
              res.statusCode = 500;
              res.statusMessage = 'Internal Server Error';
              _loggerInstance.default.error('Error applying HttpOnly session cookies', {
                namespace: logNamespace,
                additionalProperties: {
                  error: error.message || error
                }
              });
              return Buffer.from(JSON.stringify({
                error: 'Internal server error',
                message: error.message || 'An error occurred processing the authentication response'
              }), 'utf8');
            }
          }
          if (typeof options.onSLASPublicProxyRes === 'function') {
            try {
              const customBuffer = options.onSLASPublicProxyRes(workingBuffer, proxyRes, req, res);
              if (customBuffer !== undefined) {
                workingBuffer = customBuffer;
              }
            } catch (error) {
              _loggerInstance.default.error('Error in custom onSLASPublicProxyRes callback', {
                namespace: logNamespace,
                additionalProperties: {
                  error
                }
              });
            }
          }
          return workingBuffer;
        } catch (error) {
          _loggerInstance.default.error('There is an error processing the response from SLAS. Returning original response.', {
            namespace: logNamespace,
            additionalProperties: {
              error,
              statusCode: proxyRes === null || proxyRes === void 0 ? void 0 : proxyRes.statusCode,
              path: req === null || req === void 0 ? void 0 : req.url
            }
          });
          return workingBuffer;
        }
      })
    }));
  },
  /**
   * @private
   */
  _setupHealthcheck(app) {
    app.get(`${_ssrNamespacePaths.healthCheckPath}`, (_, res) => res.set('cache-control', _constants.NO_CACHE).sendStatus(200).end());
  },
  /**
   * @private
   */
  _setupCommonMiddleware(app, options) {
    app.use(prepNonProxyRequest);

    // Storefront Preview marker — when HttpOnly session cookies are
    // enabled and the incoming request looks like an iframe document
    // load from a trusted parent, set a marker cookie so later SLAS
    // proxy responses know to issue session cookies as
    // SameSite=None; Partitioned (CHIPS) instead of SameSite=Lax.
    if (process.env.MRT_ENABLE_HTTPONLY_SESSION_COOKIES === 'true') {
      app.use((req, res, next) => {
        (0, _previewContext.tryWriteStorefrontPreviewMarker)(req, res);
        next();
      });
    }

    // Apply the SSR middleware to any subsequent routes that we expect users
    // to add in their projects, like in any regular Express app.
    app.use(ssrMiddleware);
    app.use(errorHandlerMiddleware);
    if (options !== null && options !== void 0 && options.encodeNonAsciiHttpHeaders) {
      app.use(encodeNonAsciiMiddleware);
    }
    applyPatches(options);
  },
  /**
   * @private
   */
  _validateConfiguration(options) {
    // Check that we are running under a compatible version of node
    /* istanbul ignore next */
    const requiredNode = new _semver.default.Range(_package.default.engines.node);
    /* istanbul ignore next */
    if (!_semver.default.satisfies(process.versions.node,
    // A string like '8.10.0'
    requiredNode)) {
      /* istanbul ignore next */
      console.warn(`Warning: You are using Node ${process.versions.node}. ` + `Your app may not work as expected when deployed to Managed ` + `Runtime servers which are compatible with Node ${requiredNode}`);
    }

    // Verify the remote environment
    if ((0, _ssrServer.isRemote)()) {
      const notFound = [];
      REMOTE_REQUIRED_ENV_VARS.forEach(envVar => {
        if (!process.env[envVar]) {
          notFound.push(envVar);
        }
      });
      if (notFound.length) {
        throw new Error(`SSR server cannot initialize: missing environment values: ${notFound.join(', ')}`);
      }
    }
    if (['http', 'https'].indexOf(options.protocol) < 0) {
      throw new Error(`Invalid local development server protocol ${options.protocol}. ` + `Valid protocols are http and https.`);
    }
    if (!options.buildDir) {
      throw new Error('The buildDir option passed to the SSR server must be a non-empty string');
    }

    // Fix up the path in case we were passed a relative one
    options.buildDir = _path.default.resolve(process.cwd(), options.buildDir);
    if (!(options.mobify instanceof Object)) {
      throw new Error('The mobify option passed to the SSR server must be an object');
    }
    const {
      sslFilePath
    } = options;
    if (!(0, _ssrServer.isRemote)() && sslFilePath && (!sslFilePath.endsWith('.pem') || !_fs.default.existsSync(sslFilePath))) {
      throw new Error('The sslFilePath option passed to the SSR server constructor ' + 'must be a path to an SSL certificate file ' + 'in PEM format, whose name ends with ".pem". ' + 'See the "cert" and "key" options on ' + 'https://nodejs.org/api/tls.html#tls_tls_createsecurecontext_options');
    }
    if (!options.strictSSL) {
      console.warn('The SSR Server has _strictSSL turned off for https requests');
    }
  },
  /**
   * @private
   */
  _addStaticAssetServing() {
    // Handled by the CDN on remote
  },
  /**
   * @private
   */
  _addDevServerGarbageCollection() {
    // This is a hook for the dev-server. The remote-server
    // does GC in a way that is awkward to extract. See _createHandler.
  },
  /**
   * Serve the service worker at `req.path`
   *
   * For best results, serve the service worker at the root of the site and
   * it must not be a redirect. We set a long value for s-maxage (to allow CDN
   * caching), plus a strong etag (for CDN-only revalidation), and to set
   * maxage to 0 to prevent browser caching.
   *
   * See https://developer.chrome.com/blog/fresher-sw/ for details on
   * efficiently serving service workers.
   *
   */
  serveServiceWorker(req, res) {
    const options = req.app.options;
    // We apply this cache-control to all responses (200 and 404)
    res.set(_constants.CACHE_CONTROL,
    // The CDN can cache for 24 hours. The browser may not cache
    // the file.
    's-maxage=86400, max-age=0');
    const workerFilePath = _path.default.join(options.buildDir, req.path);

    // If there is no file, send a 404
    if (!_fs.default.existsSync(workerFilePath)) {
      res.status(404).send();
      return;
    }
    const content = _fs.default.readFileSync(workerFilePath, {
      encoding: 'utf8'
    });

    // If the service worker is not updated when content security policy headers inside
    // ssr.js are changed, then service worker initiated requests will continue to use
    // the old CSP headers.
    //
    // This is problematic in stacked CDN setups where an old service worker with
    // old CSPs can remain cached if the content of the service worker itself is not changed.
    //
    // To ensure the service worker is refetched when CSPs are changed, we factor in
    // the CSP headers when generating the Etag.
    //
    // See https://gus.lightning.force.com/lightning/r/ADM_Work__c/a07EE000025yeu9YAA/view
    // and https://salesforce-internal.slack.com/archives/C01GLHLBPT5/p1730739370922629
    // for more details.

    const contentSecurityPolicyHeader = res.getHeaders()[_constants.CONTENT_SECURITY_POLICY] || '';

    // Serve the file, with a strong ETag
    // For this to be a valid ETag, the string must be placed between  ""
    // See https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/ETag#etag_value for
    // more details
    res.set('etag', `"${(0, _ssrServer.getHashForString)(content + contentSecurityPolicyHeader)}"`);
    res.set(_constants.CONTENT_TYPE, 'application/javascript');
    res.send(content);
  },
  /**
   * Serve static files from the app's build directory and set default
   * cache-control headers.
   * @since v2.0.0
   *
   * @param {String} filePath - the location of the static file relative to the build directory
   * @param {Object} opts - the options object to pass to the original `sendFile` method
   */
  serveStaticFile(filePath, opts = {}) {
    return (req, res) => {
      const baseDir = req.app.options.buildDir;
      return this._serveStaticFile(req, res, baseDir, filePath, opts);
    };
  },
  /**
   * @private
   */
  _serveStaticFile(req, res, baseDir, filePath, opts = {}) {
    const options = req.app.options;
    const file = _path.default.resolve(baseDir, filePath);
    res.sendFile(file, _objectSpread({
      headers: {
        [_constants.CACHE_CONTROL]: options.defaultCacheControl
      }
    }, opts));
  },
  /**
   * Server side rendering entry.
   *
   * @since v2.0.0
   *
   * This is a wrapper around the Express `res.sendFile` method.
   *
   * @param {Object} req - the req object
   * @param {Object} req - the res object
   * @param {function} next - the callback function for middleware chain
   */
  render(req, res, next) {
    const app = req.app;
    if (!app.__renderer) {
      // See - https://www.npmjs.com/package/webpack-hot-server-middleware#usage
      const {
        buildDir
      } = app.options;
      const _require = eval('require');
      const serverRenderer = _require(_path.default.join(buildDir, 'server-renderer.js')).default;
      const stats = _require(_path.default.join(buildDir, 'loadable-stats.json'));
      app.__renderer = serverRenderer(stats);
    }
    app.__renderer(req, res, next);
  },
  /**
   * Builds a Lambda handler function from an Express app.
   *
   * See: https://docs.aws.amazon.com/lambda/latest/dg/nodejs-prog-model-handler.html
   *
   * @param app {Express} - an Express App
   * @private
   */
  _createHandler(app, options) {
    // This flag is initially false, and is set true on the first request
    // handled by a Lambda. If it is true on entry to the handler function,
    // it indicates that the Lambda container has been reused.
    let lambdaContainerReused = false;
    const serverlessAdapterHandler = _serverlessAdapter.ServerlessAdapter.new(app).setBinarySettings({
      isBinary: isBinary
    }).setFramework(new _express3.ExpressFramework()).setHandler(new _default.DefaultHandler()).setResolver(new _callback.CallbackResolver()).addAdapter(new _aws.ApiGatewayV1Adapter({
      // Preserve the original aws-serverless-express behavior
      lowercaseRequestHeaders: true,
      throwOnChunkedTransferEncoding: false
    })).build();
    const handler = (event, context, callback) => {
      // encode non ASCII request headers
      if (options !== null && options !== void 0 && options.encodeNonAsciiHttpHeaders) {
        Object.keys(event.headers).forEach(key => {
          if (!isASCII(event.headers[key])) {
            event.headers[key] = encodeURIComponent(event.headers[key]);
            // x-encoded-headers keeps track of which headers have been modified and encoded
            if (event.headers[_constants.X_ENCODED_HEADERS]) {
              // append header key
              event.headers[_constants.X_ENCODED_HEADERS] = `${event.headers[_constants.X_ENCODED_HEADERS]},${key}`;
            } else {
              event.headers[_constants.X_ENCODED_HEADERS] = key;
            }
          }
        });
      }

      // We don't want to wait for an empty event loop once the response
      // has been sent. Setting this to false will "send the response
      // right away when the callback executes", but any pending events
      // may be executed if the Lambda container is then reused for
      // another invocation (which we expect will happen under all
      // but very low load). This means two things:
      // 1. Any code that we have *after* the callback MAY be executed
      // if the Lambda container is reused, but there's no guarantee
      // it will be.
      // 2. There is no way to have code do cleanup work (such as sending
      // metrics) after the response is sent to the browser. We have
      // to accept that doing such work delays the response.
      // It would be good if we could set this to true and do work like sending
      // metrics after calling the callback, but that doesn't work - API Gateway
      // will wait for the Lambda invocation to complete before sending
      // the response to the browser.
      context.callbackWaitsForEmptyEventLoop = false;
      if (lambdaContainerReused) {
        const forceGarbageCollection = process.env.FORCE_GC;
        if (forceGarbageCollection && forceGarbageCollection.toLowerCase() === 'true') {
          // DESKTOP-434 If this Lambda container is being reused,
          // clean up memory now, so that we start with low usage.
          // These regular GC calls take about 80-100 mS each, as opposed
          // to forced GC calls, which occur randomly and can take several
          // hundred mS.
          app._collectGarbage();
        }
        // LambdaReused custom metric removed (W-22715301)
      } else {
        // This is the first use of this container, so set the
        // reused flag for next time.
        lambdaContainerReused = true;
        // LambdaCreated custom metric removed (W-22715301)
      }
      const managedCallback = (err, response) => {
        return app._requestMonitor._waitForResponses().then(() => app.metrics.flush())
        // Now call the Lambda callback to complete the response
        .then(() => callback(err, (0, _ssrServer.processLambdaResponse)(response, event)))
        // DON'T add any then() handlers here, after the callback.
        // They won't be called after the response is sent, but they
        // *might* be called if the Lambda container running this code
        // is reused, which can lead to odd and unpredictable
        // behaviour.
        ;
      };
      return serverlessAdapterHandler(event, context, managedCallback);
    };
    // Upgrading to serverless-adapter removes the server property
    // return a null server to maintain backwards compatibility
    return {
      handler,
      server: null,
      app
    };
  },
  /**
   * Create an SSR (Server-Side Rendering) Server.
   *
   * @constructor
   * @param {Object} options
   * @param {String} [options.buildDir] - The build directory path, either as an
   * absolute path, or relative to the current working directory. Defaults
   * to 'build'.
   * @param {Number} [options.defaultCacheTimeSeconds=600] - The cache time
   * for rendered pages and assets (not used in local development mode).
   * @param {Object} options.mobify - The 'mobify' object from the project's
   * package.json file, containing the SSR parameters.
   * @param {Number} [options.port=3443] - the localhost port on which the local
   * development Express app listens.
   * @param {String} [options.protocol='https'] - the protocol on which the development
   * Express app listens.
   * @param {Boolean} [options.proxyKeepAliveAgent] - This boolean value indicates
   * whether or not we are using a keep alive agent for proxy connections. Defaults
   * to 'true'. NOTE: This keep alive agent will only be used on remote.
   * @param {String} options.sslFilePath - the absolute path to a PEM format
   * certificate file to be used by the local development server. This should
   * contain both the certificate and the private key.
   * @param {function} customizeApp - a callback that takes an express app
   * as an argument. Use this to customize the server.
   * @param {Boolean} [options.allowCookies] - This boolean value indicates
   * whether or not we strip cookies from requests and block setting of cookies. Defaults
   * to 'false'.
   * @param {Boolean} [options.useSLASPrivateClient=false] - Enable the SLAS private client
   * proxy handler. Requires PWA_KIT_SLAS_CLIENT_SECRET environment variable.
   * @param {function} [options.onSLASPrivateProxyReq] - Custom callback to modify SLAS private client
   * proxy requests. Called after built-in request handling. Signature: (proxyRequest, incomingRequest, res) => void.
   * Use this to add custom headers or modify the proxy request.
   * @param {function} [options.onSLASPrivateProxyRes] - Custom callback to modify SLAS private client
   * proxy responses. Called after built-in response handling. Signature: (responseBuffer, proxyRes, req, res) => Buffer.
   * Should return the modified buffer or undefined to use the existing buffer.
   */
  createHandler(options, customizeApp) {
    process.on('unhandledRejection', _ssrServer.catchAndLog);
    const app = this._createApp(options);
    customizeApp(app, options);
    return this._createHandler(app, options);
  },
  /**
   * @private
   */
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _getRequestProcessor(req) {
    return null;
  }
};

/**
 * ExpressJS middleware that processes any non-proxy request passing
 * through the Express app.
 *
 * If allowCookies is false, strips Cookie headers from incoming requests, and
 * configures the Response so that it cannot have cookies set on it.
 *
 * Sets the Host header to the application host.
 * If there's an Origin header, rewrites it to be the application
 * Origin.
 *
 * This function should not be called for proxied requests, which
 * MAY allow use of cookies.
 *
 * @private
 */
const prepNonProxyRequest = (req, res, next) => {
  const options = req.app.options;
  if (!options.allowCookies) {
    // Strip cookies from the request
    delete req.headers.cookie;
    // In an Express Response, all cookie setting ends up
    // calling setHeader, so we override that to allow us
    // to intercept and discard cookie setting.
    const setHeader = Object.getPrototypeOf(res).setHeader;
    const remote = (0, _ssrServer.isRemote)();
    res.setHeader = function (header, value) {
      /* istanbul ignore else */
      if (header && header.toLowerCase() !== _constants.SET_COOKIE && value) {
        setHeader.call(this, header, value);
      } /* istanbul ignore else */else if (!remote) {
        _loggerInstance.default.warn(`Req ${res.locals.requestId}: ` + `Cookies cannot be set on responses sent from ` + `the SSR Server. Discarding "Set-Cookie: ${value}"`, {
          namespace: 'RemoteServerFactory.prepNonProxyRequest'
        });
      }
    };
  }

  // Set the Host header
  req.headers.host = options.appHostname;

  // Replace any Origin header
  if (req.headers.origin) {
    req.headers.origin = options.appOrigin;
  }
  next();
};

/**
 * Express Middleware applied to requests that require rendering of a response.
 *
 * @private
 */
const ssrMiddleware = (req, res, next) => {
  // The RenderTime custom metric previously emitted here was removed to
  // eliminate per-request PutMetricData cost (W-22715301).
  setDefaultHeaders(req, res);
  next();
};

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const errorHandlerMiddleware = (err, req, res, next) => {
  // The RenderErrors custom metric previously emitted here was removed to
  // eliminate per-request PutMetricData cost (W-22715301).
  (0, _ssrServer.catchAndLog)(err);
  res.sendStatus(500);
};

/**
 * Helper function that checks if a string is composed of ASCII characters
 * We only check printable ASCII characters and not special ASCII characters
 * such as NULL
 *
 * @private
 */
const isASCII = str => {
  return /^[\x20-\x7E]*$/.test(str);
};

/**
 * Express Middleware applied to responses that encode any non ASCII headers
 *
 * @private
 */
const encodeNonAsciiMiddleware = (req, res, next) => {
  const originalSetHeader = res.setHeader;
  res.setHeader = function (key, value) {
    if (!isASCII(value)) {
      originalSetHeader.call(this, key, encodeURIComponent(value));
      let encodedHeaders = res.getHeader(_constants.X_ENCODED_HEADERS);
      encodedHeaders = encodedHeaders ? `${encodedHeaders},${key}` : key;
      originalSetHeader.call(this, _constants.X_ENCODED_HEADERS, encodedHeaders);
    } else {
      originalSetHeader.call(this, key, value);
    }
  };
  next();
};

/**
 * Wrap the function fn in such a way that it will be called at most once. Subsequent
 * calls will always return the same value.
 *
 * @private
 */
const once = fn => {
  let result;
  return (...args) => {
    if (fn) {
      result = fn(...args);
      fn = null;
    }
    return result;
  };
};
exports.once = once;
const applyPatches = once(options => {
  // If we're running remotely, we also override the send()
  // method for ExpressJS's Response class (which is actually
  // a function). See responseSend for details.
  if ((0, _ssrServer.isRemote)()) {
    // http.ServerResponse.prototype
    const expressResponse = _express.default.response;
    expressResponse.send = (0, _ssrServer.responseSend)(expressResponse.send);
  }

  // Patch the http.request/get and https.request/get
  // functions to allow us to intercept them (since
  // there are multiple ways to make requests in Node).
  _http.default.request = (0, _ssrServer.outgoingRequestHook)(_http.default.request, options);
  _http.default.get = (0, _ssrServer.outgoingRequestHook)(_http.default.get, options);
  _https.default.request = (0, _ssrServer.outgoingRequestHook)(_https.default.request, options);
  _https.default.get = (0, _ssrServer.outgoingRequestHook)(_https.default.get, options);

  // We no longer use aws-serverless-express but to maintain compatibility with the old code, we still patch the redirect function.
  // - Patch the ExpressJS Response class's redirect function to suppress
  // - the creation of a body (DESKTOP-485). Including the body may
  // - trigger a parsing error in aws-serverless-express.
  _express.default.response.redirect = function (status, url) {
    let workingStatus = status;
    let workingUrl = url;
    if (typeof status === 'string') {
      workingUrl = status;
      workingStatus = 302;
    }

    // Duplicate behaviour in node_modules/express/lib/response.js
    const address = this.location(workingUrl).get('Location');

    // aws-serverless-express would add a Content-Length header to the response even if there was no body
    this.set('Content-Length', 0);

    // Send a minimal response with just a status and location
    this.status(workingStatus).location(address).end();
  };

  // Patch the whatwg-encoding decode function so that it will accept plain
  // JS strings and return them as-is.
  // eslint-disable-next-line @typescript-eslint/no-var-requires
  const whatWGEncoding = require('whatwg-encoding');
  const originalDecode = whatWGEncoding.decode;
  whatWGEncoding.decode = (buffer, fallbackEncodingName) => {
    /* istanbul ignore else */
    if (typeof buffer === 'string') {
      return buffer;
    }
    /* istanbul ignore next */
    return originalDecode(buffer, fallbackEncodingName);
  };

  // The 'http-proxy-middleware' module pollutes global by adding a lodash
  // reference to it as '_' (this global reference is not used). We kill
  // that reference here because otherwise it can hold on to references
  // to other objects.
  /* istanbul ignore next */
  if (global._) {
    delete global._;
  }
});

/**
 * Set default headers on a response. The arguments to this function
 * are the same as those for the responseHook function.
 *
 * @private
 */
const setDefaultHeaders = (req, res) => {
  const requestClass = res.locals.requestClass;
  if (requestClass) {
    res.set(_ssrProxying.X_MOBIFY_REQUEST_CLASS, requestClass);
  }
};

/**
 * Tracks in-flight requests.
 *
 * @private
 */
class RequestMonitor {
  constructor() {
    this._pendingResponses = {
      ids: [],
      promise: null,
      resolve: null
    };
  }
  /**
   * Returns a Promise that wil resolve when the server has completed
   * handling of any current requests. If the server is idle, returns
   * a Promise that is already resolved.
   *
   * This method is safe to use at any point. If there are any responses
   * in progress when it's called, then the returned Promise will not
   * resolve until all responses complete, even if those responses start
   * after this method is called.
   *
   * @private
   * @returns {Promise}
   */
  _waitForResponses() {
    const pending = this._pendingResponses;
    if (pending.ids.length === 0) {
      return _express2.RESOLVED_PROMISE;
    }
    if (!pending.promise) {
      pending.promise = new Promise(resolve => {
        pending.resolve = resolve;
      });
    }
    return pending.promise;
  }

  /**
   * Works with waitForCompletion: when invoked by the initial request
   * handler, adds the id of the response to the set of pending ids.
   * @param res {express.response} the response that has started
   * @private
   */
  _responseStarted(res) {
    this._pendingResponses.ids.push(res.locals.requestId);
    const finish = () => this._responseFinished(res);
    // We hook both the 'finished' and 'close' events, so that
    // we properly complete any responses that fail (in which case
    // 'close' will fire, but 'finish' may not).
    res.once('finish', finish);
    res.once('close', finish);
  }

  /**
   * Works with waitForCompletion: when invoked by the 'finish' event of
   * any response, removes the response from the list of those pending
   * (using the id, so that multiple calls to this method will work
   * correctly). If there are no more responses pending, resolves any
   * Promise that is waiting for responses to complete.
   * @param res {express.response} the response that has finished
   * @private
   */
  _responseFinished(res) {
    const pending = this._pendingResponses;
    if (pending.ids.length) {
      const requestId = res.locals.requestId;
      pending.ids = pending.ids.filter(id => id !== requestId);
      if (pending.ids.length === 0 && pending.resolve) {
        pending.resolve();
        pending.resolve = pending.promise = null;
      }
    }
  }
}