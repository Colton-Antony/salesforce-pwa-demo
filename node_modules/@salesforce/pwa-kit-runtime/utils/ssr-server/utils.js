"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.verboseProxyLogging = exports.setQuiet = exports.parseRequestUrl = exports.parseCacheControl = exports.logMRTError = exports.localDevLog = exports.isString = exports.isScapiDomain = exports.isRemote = exports.isQuiet = exports.isObject = exports.isIterable = exports.isArray = exports.infoLog = exports.getHashForString = exports.getFullRequestURL = exports.getBundleBaseUrl = exports.forEachIn = exports.escapeJSText = exports.catchAndLog = void 0;
var _crypto = _interopRequireDefault(require("crypto"));
var _ssrShared = require("../ssr-shared");
var _ssrNamespacePaths = require("../ssr-namespace-paths");
var _loggerInstance = _interopRequireDefault(require("../logger-instance"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); } /*
 * Copyright (c) 2022, Salesforce, Inc.
 * All rights reserved.
 * SPDX-License-Identifier: BSD-3-Clause
 * For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/BSD-3-Clause
 */ // To avoid an unwieldy file size, this file should be for utils that are less than ~20 lines.
// Larger utils should be placed in separate files.
// Also, ./utils/ssr-server/utils is a bit of a silly name, but these helpers can't live in
// ../ssr-server.js because it would create circular dependencies.
// TODO: Clean this up or provide a way to toggle
const verboseProxyLogging = exports.verboseProxyLogging = false;
const isRemote = () => Object.prototype.hasOwnProperty.call(process.env, 'AWS_LAMBDA_FUNCTION_NAME');
exports.isRemote = isRemote;
const getBundleBaseUrl = () => {
  return `${_ssrNamespacePaths.bundleBasePath}/${isRemote() ? process.env.BUNDLE_ID : 'development'}/`;
};
exports.getBundleBaseUrl = getBundleBaseUrl;
let QUIET = false;
const setQuiet = quiet => {
  QUIET = quiet;
};
exports.setQuiet = setQuiet;
const isQuiet = () => QUIET;

// Logs in local development server mode only
exports.isQuiet = isQuiet;
const localDevLog = (...args) => {
  if (!isRemote() && !QUIET) {
    /* istanbul ignore next */
    console.log(...args);
  }
};

// Logs unless server is in quiet mode (used for tests)
exports.localDevLog = localDevLog;
const infoLog = (...args) => {
  /* istanbul ignore next */
  if (!QUIET) {
    /* istanbul ignore next */
    console.log(...args);
  }
};

/**
 * A global error catcher
 * @private
 */
exports.infoLog = infoLog;
const catchAndLog = (err, context) => {
  /* istanbul ignore next */
  const message = `${context || 'Uncaught exception'}: `;
  _loggerInstance.default.error(message, /* istanbul ignore next */
  {
    namespace: 'catchAndLog',
    additionalProperties: {
      stack: err && (err.stack || err.message || err) || '(no error)'
    }
  });
};

/**
 * Given a piece of JavaScript or JSON as text, escape any
 * '</' so that it can be embedded within HTML.
 *
 * @private
 * @param {String} text
 * @returns {String}
 */
exports.catchAndLog = catchAndLog;
const escapeJSText = text => text === null || text === void 0 ? void 0 : text.replace(/<\//gm, '\\x3c\\x2f');
exports.escapeJSText = escapeJSText;
const getHashForString = text => {
  const hash = _crypto.default.createHash('sha256');
  hash.update(text);
  return hash.digest('hex');
};
exports.getHashForString = getHashForString;
const getFullRequestURL = url => {
  // If it starts with a protocol (e.g. http(s)://, file://), then it's already a full URL
  if (/^[a-zA-Z]+:\/\//.test(url)) return url;
  const proxy = _ssrShared.proxyConfigs.find(({
    path
  }) => url.startsWith(`${_ssrNamespacePaths.proxyBasePath}/${path}/`));
  if (proxy) {
    return url.replace(`${_ssrNamespacePaths.proxyBasePath}/${proxy.path}`, `${proxy.protocol}://${proxy.host}`);
  }
  throw new Error(`Unable to fetch ${url}, relative paths must begin with ${_ssrNamespacePaths.proxyBasePath} followed by a configured proxy path.`);
};
exports.getFullRequestURL = getFullRequestURL;
const CC_AGE_RE = /(s-maxage|max-age)\s*=\s*(\d+)/gi;

/**
 * Perform limited parsing of a Cache-Control header value, to
 * extract the s-maxage and max-age values and return them.
 *
 * @function
 * @param value {String} the value to parse
 * @returns {Object} with 'max-age' and 's-maxage' properties mapped
 * to String or undefined values.
 */
const parseCacheControl = value => {
  const result = {};
  if (value) {
    for (const match of value.matchAll(CC_AGE_RE)) {
      result[match[1].toLowerCase()] = match[2];
    }
  }
  return result;
};

/**
 * Parse a request URL using the WHATWG URL constructor, constructing a
 * base URL from the request's protocol and host header when available.
 * Falls back to 'http://localhost' as the base when host information is
 * not present — in that case, do not rely on `origin` or `href` from
 * the returned URL since they will contain the placeholder base.
 *
 * @param {Object} req - Express-like request object with `url`, `protocol`,
 *   `headers.host`, and optionally `socket.encrypted` properties.
 * @returns {{pathname: string, search: string, query: string|null}}
 *   `pathname` – the URL path, `search` – the full query string including
 *   leading `?` (or empty string), `query` – the query string without `?`
 *   (or null if there is none).
 */
exports.parseCacheControl = parseCacheControl;
const parseRequestUrl = req => {
  var _req$socket, _req$headers;
  const proto = req.protocol || ((_req$socket = req.socket) !== null && _req$socket !== void 0 && _req$socket.encrypted ? 'https' : 'http');
  const base = `${proto}://${((_req$headers = req.headers) === null || _req$headers === void 0 ? void 0 : _req$headers.host) || 'localhost'}`;
  const {
    pathname,
    search
  } = new URL(req.url, base);
  return {
    pathname,
    search,
    query: search ? search.slice(1) : null
  };
};

/**
 * Type checking utility functions
 */
exports.parseRequestUrl = parseRequestUrl;
const isString = element => typeof element === 'string';
exports.isString = isString;
const isArray = element => Array.isArray(element);
exports.isArray = isArray;
const isObject = element => element !== null && typeof element === 'object' && !Array.isArray(element);
exports.isObject = isObject;
const isIterable = element => isArray(element) || isObject(element);

/**
 * Iterate over an object or array, calling a function for each key-value pair
 * @param {Object|Array} iterable - The object or array to iterate over
 * @param {Function} functionRef - Function to call for each key-value pair (key, value)
 */
exports.isIterable = isIterable;
const forEachIn = (iterable, functionRef) => {
  Object.keys(iterable).forEach(key => {
    functionRef(key, iterable[key]);
  });
};

/**
 * Check if the target host is a Salesforce Commerce API domain
 * @param {string} targetHost - The target host (may include port, e.g., "host.com:443")
 * @returns {boolean} True if it's an SCAPI domain
 */
exports.forEachIn = forEachIn;
const isScapiDomain = targetHost => {
  if (!targetHost) return false;

  // Remove port if present (handle both IPv4 and domain formats)
  // Example: "abc-001.api.commercecloud.salesforce.com:443" -> "abc-001.api.commercecloud.salesforce.com"
  const hostname = targetHost.split(':')[0];

  // Check if it matches *.api.commercecloud.salesforce.com pattern
  // SCAPI domains always have an instance identifier subdomain (e.g., abc-001, kv7kzm78)
  return hostname.endsWith('.api.commercecloud.salesforce.com');
};

/**
 * Log an internal MRT error.
 *
 * @param namespace Namespace for the error (e.g. data_store, redirect) to facilitate searching
 * @param err Error to log
 * @param context Optional context to include in the log
 */
exports.isScapiDomain = isScapiDomain;
const logMRTError = (namespace, err, context) => {
  const error = err instanceof Error ? err : new Error(String(err));
  console.error(JSON.stringify(_objectSpread({
    [`__MRT__${namespace}`]: 'error',
    type: 'MRT_internal',
    error: error.message,
    stack: error.stack
  }, context)));
};
exports.logMRTError = logMRTError;