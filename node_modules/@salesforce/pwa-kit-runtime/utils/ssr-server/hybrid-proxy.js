"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.hybridProxy = hybridProxy;
exports.iterate = void 0;
var _utils = require("./utils");
var _httpProxyMiddleware = require("http-proxy-middleware");
var _loggerInstance = _interopRequireDefault(require("../logger-instance"));
var _mrtRuleMatcher = require("./mrt-rule-matcher");
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; } /*
 * Copyright (c) 2022, Salesforce, Inc.
 * All rights reserved.
 * SPDX-License-Identifier: BSD-3-Clause
 * For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/BSD-3-Clause
 */
/**
 * Express middleware that proxies hybrid traffic to an SFCC instance
 *
 * @param {Object} options runtime options
 */
function hybridProxy(options) {
  var _options$hybridProxy, _options$hybridProxy2;
  var proxyOptions = {};
  var hybridRoutingRules = [];
  if (!options.localAllowCookies) {
    _loggerInstance.default.warn('WARNING: options.localAllowCookies is not set to true. SFCC sessions will not work.');
  }

  // Target SFCC Instance to Proxy Requests to
  // ex. https://abcd-002.dx.commercecloud.salesforce.com
  const sfccOrigin = (_options$hybridProxy = options.hybridProxy) === null || _options$hybridProxy === void 0 ? void 0 : _options$hybridProxy.sfccOrigin;
  if (!sfccOrigin) {
    _loggerInstance.default.warn('WARNING: options.hybridProxy.sfccOrigin is required to use hybrid proxy. Please set it in your server options.');
  }

  // Ourselves, the pwa-kit application
  const appHostname = options.appHostname;
  const protocol = (0, _utils.isRemote)() ? 'https' : options.protocol;
  const proxyOrigin = `${protocol}://${appHostname}`;
  // Gather Hybrid routing rules from options. If the rule is not matched, we will proxy the request to sfccOrigin.
  hybridRoutingRules = ((_options$hybridProxy2 = options.hybridProxy) === null || _options$hybridProxy2 === void 0 ? void 0 : _options$hybridProxy2.routingRules) || [];
  if (hybridRoutingRules.length === 0) {
    _loggerInstance.default.warn('WARNING: No hybridProxy.routingRules found. Please set options.hybridProxy.routingRules in your server options.');
  }
  proxyOptions = {
    target: sfccOrigin,
    changeOrigin: true,
    // rewrite Location headers
    autoRewrite: true,
    hostRewrite: true,
    cookieDomainRewrite: true,
    selfHandleResponse: true,
    onProxyRes: (proxyRes, req, res) => {
      return (0, _httpProxyMiddleware.responseInterceptor)(/*#__PURE__*/function () {
        var _ref = _asyncToGenerator(function* (responseBuffer) {
          var _proxyRes$headers, _proxyRes$headers2;
          const contentType = proxyRes === null || proxyRes === void 0 ? void 0 : proxyRes.headers['content-type'];
          if (!contentType) return responseBuffer;
          let response;
          let updatedResponse;
          switch (contentType.split(';')[0]) {
            case 'text/html':
              response = responseBuffer.toString('utf8');

              // some links are absolute URLs, replace them so they go through the proxy
              updatedResponse = response.replace(new RegExp(`${sfccOrigin}`, 'g'), proxyOrigin);

              // replace any redirects to the SFCC origin with the proxy origin
              if (proxyRes !== null && proxyRes !== void 0 && (_proxyRes$headers = proxyRes.headers) !== null && _proxyRes$headers !== void 0 && _proxyRes$headers.location && proxyRes !== null && proxyRes !== void 0 && (_proxyRes$headers2 = proxyRes.headers) !== null && _proxyRes$headers2 !== void 0 && _proxyRes$headers2.location.includes(sfccOrigin)) {
                _loggerInstance.default.info(`Rewriting location header => ${proxyRes.headers.location}`);
                res.setHeader('location', proxyRes.headers.location.replace(sfccOrigin, proxyOrigin));
              }

              // prefix /on/demandware.static/ with /mobify/proxy/dwrestatic to force
              // static assets through an MRT proxy; there should be no reason
              // to process this in runtime code (nor should there be POST requests for these)
              // requires a unique MRT proxy configure; ignore DIS CDN links that look similar
              updatedResponse = updatedResponse.replace(new RegExp(`(?<!dw/image/.+?/.+?)/on/demandware\\.static/`, 'g'), `/mobify/proxy/dwrestatic/on/demandware.static/`);
              return updatedResponse;
            case 'application/json':
              try {
                response = JSON.parse(responseBuffer.toString('utf8'));
                return JSON.stringify(iterate(response, null, {
                  sfccOrigin,
                  proxyOrigin
                }));
              } catch (e) {
                _loggerInstance.default.error(`error parsing JSON input: ${e}`);
                return responseBuffer;
              }
            default:
              return responseBuffer;
          }
        });
        return function (_x) {
          return _ref.apply(this, arguments);
        };
      }())(proxyRes, req, res);
    }
  };

  // Attach the proxy middleware if we are in hybrid mode
  return (0, _httpProxyMiddleware.createProxyMiddleware)(function (pathname, req) {
    let match = hybridRoutingRules.some(rule => (0, _mrtRuleMatcher.evaluateRule)(rule, {
      host: req.hostname,
      uri: req.url,
      path: pathname,
      cookies: req.headers.cookie || ''
    }));

    // hybridRoutingRules(MRT eCDN rules) are evaluated to determine what gets sent to MRT
    // https://developer.salesforce.com/docs/commerce/commerce-api/references/cdn-api-process-apis?meta=createMrtRules
    // So the traffic we proxy to SFCC will be the opposite
    return !match;
  }, proxyOptions);
}

/**
 * This key is used to identify JSON properties that contain URLs
 * that need to be rewritten from SFCC origin to proxy origin.
 */
const KEY_TO_REWRITE = 'redirecturl';
const iterate = (object, parent, vars = {}) => {
  if (!(0, _utils.isIterable)(object)) return object;
  const {
    sfccOrigin,
    proxyOrigin
  } = vars;
  (0, _utils.forEachIn)(object, (key, value) => {
    // replace any urls to the SFCC origin with the proxy origin
    if ((0, _utils.isString)(value) && (0, _utils.isString)(key) && String(key).toLowerCase() === KEY_TO_REWRITE) {
      _loggerInstance.default.info(`Rewriting JSON value => ${value} for key: ${key}`);
      object[key] = value.replace(sfccOrigin, proxyOrigin);
      _loggerInstance.default.info(`new value => ${object[key]}`);
    }
    iterate(value, parent, vars);
  });
  return object;
};
exports.iterate = iterate;