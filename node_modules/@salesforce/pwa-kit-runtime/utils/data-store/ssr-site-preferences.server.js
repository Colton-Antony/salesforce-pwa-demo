"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.buildCustomSitePreferencesDataStoreKey = buildCustomSitePreferencesDataStoreKey;
exports.getCustomSitePreferences = getCustomSitePreferences;
var _constants = require("./constants");
var _dataStoreUtils = require("./data-store-utils");
function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; } /*
 * Copyright (c) 2026, Salesforce, Inc.
 * All rights reserved.
 * SPDX-License-Identifier: BSD-3-Clause
 * For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/BSD-3-Clause
 */
/**
 * Build the Data Store key for custom site preferences (DAL).
 * @param {string | undefined} siteId - Commerce site id
 * @returns {string | null}
 */
function buildCustomSitePreferencesDataStoreKey(siteId) {
  if (!siteId || typeof siteId !== 'string') {
    return null;
  }
  return `${siteId}${_constants.CUSTOM_SITE_PREFERENCES_KEY_SUFFIX}`;
}

/**
 * Fetch custom site preferences from the MRT Data Store for SSR (server-only async API).
 * Returns `{}` when the store is unavailable, the key is missing, or site id is unknown.
 *
 * @param {{ siteId?: string }} params
 * @returns {Promise<Record<string, unknown>>}
 */
function getCustomSitePreferences(_x) {
  return _getCustomSitePreferences.apply(this, arguments);
}
function _getCustomSitePreferences() {
  _getCustomSitePreferences = _asyncToGenerator(function* ({
    siteId
  }) {
    const key = buildCustomSitePreferencesDataStoreKey(siteId);
    return (0, _dataStoreUtils.getPlainObjectForDataStoreKey)({
      dataStoreKey: key,
      logNamespace: 'custom-site-preferences',
      serviceErrorMessage: 'Custom site preferences Data Store request failed'
    });
  });
  return _getCustomSitePreferences.apply(this, arguments);
}