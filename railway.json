{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install @formatjs/cli && npm run build"
  },
  "deploy": {
    "startCommand": "chmod -R +x node_modules && npm start"
  }
}