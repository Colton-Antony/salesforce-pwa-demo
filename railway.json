{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install @formatjs/cli && chmod -R +x node_modules/.bin && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start:prod"
  }
}