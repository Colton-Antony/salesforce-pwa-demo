{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install -g @formatjs/cli && npm run build"
  },
  "deploy": {
    "startCommand": "npm start"
  }
}